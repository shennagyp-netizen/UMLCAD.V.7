\documentclass[11pt,a4paper]{report}

\usepackage[a4paper,margin=22mm,headheight=14pt]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath}
\usepackage{array}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{ragged2e}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,calc}
\usepackage[hidelinks]{hyperref}
\usepackage{fancyhdr}
\usepackage{titlesec}

\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}
\setlist{leftmargin=*,itemsep=2pt,topsep=2pt}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\setlength{\emergencystretch}{2em}
\definecolor{codegray}{gray}{0.96}
\definecolor{rulegray}{gray}{0.55}
\lstset{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{codegray},
  frame=single,
  rulecolor=\color{rulegray},
  breaklines=true,
  breakatwhitespace=false,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  xleftmargin=4pt,
  xrightmargin=4pt
}
\newcommand{\pathref}[1]{\url{#1}}
\newcommand{\status}[1]{\textbf{#1}}
\newcommand{\current}{\textit{Current main implementation}}
\newcommand{\target}{\textit{System-CAD target architecture}}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{UMLCAD.V.7}
\fancyhead[R]{System Architecture Handbook}
\fancyfoot[C]{\thepage}

\titleformat{\chapter}{\LARGE\bfseries}{\thechapter}{10pt}{}
\titleformat{\section}{\Large\bfseries}{\thesection}{8pt}{}
\titleformat{\subsection}{\large\bfseries}{\thesubsection}{6pt}{}

\begin{document}

\begin{titlepage}
\centering
\vspace*{18mm}
{\Huge\bfseries UMLCAD.V.7\par}
\vspace{5mm}
{\LARGE System Architecture, Mathematical Authority, Engineering Domains, Contracts,\par}
{\LARGE Runtime Boundaries, Validation, and Repository Reference\par}
\vspace{15mm}
{\large Comprehensive engineering handbook\par}
\vspace{6mm}
{\large Audited against the exact repository state immediately before this document revision:\par}
\vspace{2mm}
{\ttfamily 2451b765eb1e1e3177b965ffa5ab23aa35a51995\par}
\vfill
{\large The repository is the source of truth.\par}
{\large This document distinguishes implemented code from target architecture.\par}
\vspace{8mm}
{\large September 19, 2026\par}
\end{titlepage}

\tableofcontents
\clearpage

\chapter{Document Purpose and Reading Rules}

This handbook documents the architecture and engineering state of UMLCAD.V.7 as a whole. It is deliberately more precise than a marketing overview and more compact than a raw source dump. The document describes the repository as an engineering system: authority, ownership, contracts, state transitions, deterministic identity, mathematical guarantees, provider boundaries, testing, deployment shape, and known limits.

There are two separate truths that must never be confused:

\begin{tabularx}{\textwidth}{@{}p{0.23\textwidth}X@{}}
\toprule
Term & Meaning \\
\midrule
\current & Code and files verified to exist in the current \texttt{main} repository state. \\
\target & Architectural direction expressed by the current development law and the architecture implied by the requested System-CAD model. It may not yet have an implementation on \texttt{main}. \\
\bottomrule
\end{tabularx}

A class, interface, endpoint, test, or documentation statement is not by itself proof that a capability is complete. Completion requires the semantic path, authoritative behavior, failure semantics, determinism, required tests, and applicable end-to-end evidence.

The mathematical roadmap M0-M16 is closed for its declared certified domains. That closure means the mathematical authority is treated as the existing foundation for the next system-CAD layer; it does not mean every possible CAD mathematical operation is implemented.

\section{Repository fact at the audit point}
At the audit point, the repository contains the mature Rust mathematical/native foundation, the existing \texttt{UMLCAD.Framework} and \texttt{UMLCAD.Kernel.Client} .NET surface, the legacy TypeScript kernel API, the OCCT backend/contracts, the demo/E2E harness, and the CI workflows described later. Some newer System-CAD domain projects described in earlier handbook text are not present on the exact current \texttt{main} tree and are therefore documented here as target architecture rather than current implementation.

\chapter{Executive Architecture}

The stable conceptual architecture is:

\begin{center}
\begin{tikzpicture}[
  box/.style={draw,rounded corners,align=center,minimum width=43mm,minimum height=10mm,font=\small},
  a/.style={box,fill=blue!6}, b/.style={box,fill=green!7}, c/.style={box,fill=orange!9}, d/.style={box,fill=gray!10},
  >=Latex
]
\node[a] (platform) {Platform Foundation};
\node[a,below=5mm of platform] (math) {Rust Mathematical Authority};
\node[b,below=5mm of math] (science) {Science};
\node[b,below=5mm of science] (cad) {CAD Core / Product Semantics};
\node[c,below=5mm of cad] (resources) {Engineering Resources};
\node[c,below=5mm of resources] (domains) {Specialized Engineering Domains};
\node[d,below=5mm of domains] (app) {Application / Workflow / Presentation};
\draw[->] (platform)--(math);
\draw[->] (math)--(science);
\draw[->] (science)--(cad);
\draw[->] (cad)--(resources);
\draw[->] (resources)--(domains);
\draw[->] (domains)--(app);
\end{tikzpicture}
\end{center}

The important point is not that every layer must be one project. The point is ownership and dependency direction.

\section{Authority hierarchy}

\begin{enumerate}
\item \textbf{Rust mathematics}: normative numerical authority for the mathematical contracts it declares and certifies.
\item \textbf{System-CAD semantics}: authoritative owner of CAD meaning, product meaning, engineering intent, references, evaluation orchestration, lifecycle state, and downstream domain composition.
\item \textbf{Providers/adapters}: implementation mechanisms behind stable service contracts.
\item \textbf{Representation/viewer}: derived consumer assets only; never authority.
\end{enumerate}

The Rust kernel is intentionally thin. The complete CAD system must not be compared with it as though they were equivalent architectural layers.

\chapter{C4 Level 1 - System Context}

\begin{center}
\begin{tikzpicture}[
  ext/.style={draw,rounded corners,align=center,minimum width=27mm,minimum height=9mm,font=\scriptsize,fill=gray!10},
  sys/.style={draw,rounded corners,align=center,minimum width=38mm,minimum height=11mm,font=\scriptsize,fill=blue!8},
  >=Latex
]
\node[ext] (engineer) {Engineer / System User};
\node[sys,right=12mm of engineer] (cadapp) {UMLCAD.V.7\ System-CAD};
\node[sys,below=11mm of cadapp] (kernel) {Rust Mathematical\ Kernel};
\node[ext,right=12mm of cadapp] (providers) {Engineering / Scientific\ Providers};
\node[ext,below=10mm of providers] (plm) {PLM / PDM / ERP};
\node[ext,below=10mm of engineer] (machine) {Manufacturing Machine / Controller};
\draw[->] (engineer)--node[above]{design / inspect} (cadapp);
\draw[->] (cadapp)--node[right]{contracted mathematics} (kernel);
\draw[<->] (cadapp)--node[above]{provider contracts} (providers);
\draw[<->] (cadapp)--node[right]{lifecycle / product data} (plm);
\draw[->] (cadapp)--node[left]{deterministic NC} (machine);
\end{tikzpicture}
\end{center}

\target{} treats PLM, simulation, machine-control, and other external systems as integrations rather than hidden dependencies inside CAD semantics.

\chapter{C4 Level 2 - Containers}

\begin{tabularx}{\textwidth}{@{}p{0.24\textwidth}X@{}}
\toprule
Container / subsystem & Responsibility \\
\midrule
\pathref{kernel/math} & CPU mathematical authority: scalar, tolerance, vector/matrix, analytic geometry, curves/NURBS, surfaces, intersections, constraints/Jacobian/solver, B-Rep/solid math, tessellation, spatial acceleration, GPU contracts, and related diagnostics. \\
\pathref{kernel/native} & Rust host and public kernel API. Dispatches typed requests to mathematical services and owns native GPU integration. \\
\pathref{kernel/contracts} & Backend-neutral Rust contracts for geometry, topology, mesh, exchange, surface operations, references, construction, and kernel lifecycle/integration behavior. \\
\pathref{kernel/occt} & OCCT-backed implementation/oracle layer. It may realize or challenge contracts but is not semantic authority. \\
\pathref{kernel/api} & Legacy TypeScript API/application surface. Useful for compatibility/history; it is not the long-term semantic authority. \\
\pathref{dotnet/src/UMLCAD.Framework} & Current .NET semantic/build framework: application builder, product-like definitions, assemblies, drawings/sheets, metadata, configuration, deterministic build identity, build history, package creation, and compiled model manifest generation. \\
\pathref{dotnet/src/UMLCAD.Kernel.Client} & Current .NET transport adapter to the Rust kernel. Handles timeout, response limits, JSON, stable diagnostics, cancellation, and compiled-result validation. \\
\pathref{projects/demo} & Executable demonstration and integrated runtime path. The E2E mode starts the kernel and validates the compiled model without opening the viewer. \\
\pathref{tests/e2e} & System-level process/wire gate and red-team harness. \\
\pathref{viewer} & Presentation boundary. The current repository proves the viewer package manifest and demo integration assumptions; implementation details that are not present on the exact \texttt{main} tree are not claimed here as implemented. \\
\bottomrule
\end{tabularx}

\chapter{System-CAD Domain Architecture}

The long-lived System-CAD architecture is organized around meaning rather than a list of applications.

\section{Core semantic ownership}

CAD Core owns, where applicable:

\begin{itemize}
\item Product, Part, Body, Sketch, Feature, Assembly, Occurrence, and product-structure semantics.
\item Design intent, parameters, constraints, references, publications, and configuration/context.
\item Coordinate-frame semantics and transforms.
\item Evaluation planning and dependency closure.
\item Deterministic semantic/evaluation/result identity.
\item Cache and invalidation semantics.
\item Authoritative results, provenance, topology evolution, and diagnostics.
\item Drawing and PMI meaning.
\item Product Structure and BOM semantics.
\item Integration contracts consumed by Sheet Metal, CAM, Simulation, and PLM.
\end{itemize}

The mathematical kernel supplies computations, not these meanings.

\section{Stable relationship model}

The requested relationship is an architecture invariant:

\begin{center}
\begin{tikzpicture}[
  node distance=7mm and 13mm,
  box/.style={draw,rounded corners,align=center,minimum width=37mm,minimum height=9mm,font=\small},
  >=Latex
]
\node[box,fill=green!8] (science) {Science\\Material / physical properties\\Phenomena Simulation Service};
\node[box,fill=orange!8,right=of science] (resources) {Engineering Resources\\Machine / Tool / Fixture\\Process / Capabilities};
\node[box,fill=blue!7,below=of science] (sheet) {Sheet Metal};
\node[box,fill=blue!7,below=of resources] (cam) {CAM\\Toolpath / Postprocessor\\Deterministic G-code / NC};
\node[box,fill=gray!10,below=15mm of $(sheet)!0.5!(cam)$] (product) {CAD Product Structure};
\node[box,fill=gray!10,below=of product] (bom) {BOM};
\node[box,fill=gray!10,below=of bom,minimum width=75mm] (consumers) {Drawing / CAM / PLM};
\draw[<->] (science)--(resources);
\draw[->] (resources)--(sheet);
\draw[->] (resources)--(cam);
\draw[->] (science)--(cam);
\draw[->] (science)--(sheet);
\draw[->] (product)--(bom);
\draw[->] (bom)--(consumers);
\draw[->] (product)--(consumers);
\end{tikzpicture}
\end{center}

The crossing arrows above express consumption relationships, not inheritance and not ownership transfer.

\section{Science}
Science owns scientific facts and services that can be reused by multiple engineering domains. Material and physical properties belong here. The Phenomena Simulation Service is an abstract, provider-neutral service boundary.

\section{Phenomena Simulation Service}

The stable boundary is:

\begin{lstlisting}
Engineering Consumer
        -> IPhenomenaSimulationService
        -> IPhenomenaSimulationProvider
        -> Simulation Implementation
\end{lstlisting}

The service does not encode whether the provider is internal, external, CPU-based, GPU-based, specialized, or remote. Provider identity, execution detail, and transport belong behind the contract unless the service explicitly makes them semantic inputs.

A simulation result must carry enough identity, inputs, assumptions, and provenance for the consumer to decide whether the result is applicable. A visually plausible scalar without provenance is not an authoritative engineering result.

\section{Engineering Resources}

Engineering Resources own reusable manufacturing/engineering facts and capability declarations:

\begin{itemize}
\item Machine.
\item Tool.
\item Fixture.
\item Process.
\item Capabilities.
\end{itemize}

CAM and Sheet Metal consume these resources; the resource model must not become hidden CAM state.

\section{Sheet Metal}

Sheet Metal owns sheet-metal design semantics. It consumes engineering resources and applicable science/simulation contracts. Material/machine/process feasibility validation is a cross-domain responsibility implemented through explicit contracts, not by allowing Sheet Metal or CAM to become a second CAD kernel.

\section{CAM}

CAM consumes authoritative CAD semantics, product structure/BOM, engineering resources, material/process information, applicable Sheet Metal outputs, and the Phenomena Simulation Service where needed.

The manufacturing chain is:

\begin{lstlisting}
Manufacturing Definition
        -> Toolpath
        -> Postprocessor
        -> Deterministic G-code / NC
\end{lstlisting}

The postprocessor is a semantic boundary. Machine configuration, tool, fixture, process, coordinate system, operation order, postprocessor version/configuration, and other declared deterministic inputs must participate in the relevant identity. A renderable toolpath is not itself machine-ready NC.

\section{CAD Product Structure and BOM}

Product Structure owns product composition. BOM is a derived semantic view:

\begin{lstlisting}
CAD Product Structure
        -> BOM
        -> Drawing / CAM / PLM
\end{lstlisting}
\begin{longtable}{@{}p{0.07\textwidth}p{0.31\textwidth}p{0.49\textwidth}@{}}
\toprule
ID & Family & Certified / recorded state \\
\midrule
M0 & Audit and baseline & Repository/math inventory and authority baseline established. \\
M1 & Scalars, constants, tolerance & Finite-safe scalar operations, constants, explicit tolerance policy. Known legacy direct arithmetic/tolerance migration remains a code-quality concern outside the claimed certified domains. \\
M2 & Vectors, matrices, quaternion, transform & Vec2/Vec3/Vec4, matrices, quaternion, affine/rigid transform infrastructure. \\
M3 & Analytic geometry and predicates & Analytic primitives and scale/degeneracy predicates. \\
M4 & Polynomial and interval & Root/polynomial and interval foundations; not every possible algebraic problem is certified. \\
M5 & Curves & Bezier/B-spline/NURBS and curve differential foundations. \\
M6 & Surfaces and NURBS & Parametric surface and NURBS surface differential/trim foundations. \\
M7 & Distance/projection/intersection & Declared primitive and bounded intersection/projection families. \\
M8 & CAD construction mathematics & Declared convex/analytic offset, extrusion, revolution, loft, pipe, blend, frame/twist, box fillet/chamfer, shell domains. \\
M9 & B-Rep and solids & Explicit topology, certified planar-face solid properties, and bounded AABB Boolean/split/imprint domains. General curved-face sewing/unrestricted Boolean construction are outside scope. \\
M10 & Constraints/Jacobian/solver & Complete analytic Jacobian for supported equation set; damped SVD/least-squares; rank/nullity/conditioning/terminal authority and mixed-unit adversarial evidence. \\
M11 & Tessellation/spatial & Adaptive curve/surface tessellation; certified convex line/arc trim-aware tessellation; AABB/sphere/subdivision/BVH acceleration. General concave/holed/freeform trim filling is excluded. \\
M12 & GPU abstraction & Backend-neutral capability, selection, batching, dispatch, executor, and conformance contracts. No GPU resource is semantic state. \\
M13 & Metal & Real Apple Silicon Metal conservative AABB candidate-generation backend; hardware-validated on Apple Silicon. Metal lacks native hardware f64, so broad authoritative f64 math remains CPU. \\
M14 & CUDA & Real CUDA device-native-f64 AABB candidate generation; standard repository CI lacks confirmed NVIDIA hardware execution, so hardware status remains unvalidated. \\
M15 & Cross-backend conformance & CPU/Metal/CUDA common AABB candidate-set conformance contract; current hardware evidence validates CPU/Metal; CUDA hardware is unvalidated. \\
M16 & Performance and final red team & Apple Silicon performance evidence plus final adversarial matrix; mathematical roadmap closed. \\
\bottomrule
\end{longtable}

\section{CPU authority}
CPU is normative. GPU code must conform to the CPU contract and cannot redefine semantics.

\section{Numerical infrastructure}
Current native numerical implementation uses \texttt{nalgebra = 0.33}. The policy is not to replace numerical infrastructure merely for novelty; a replacement requires a demonstrated correctness/architecture reason and a verification plan.

\chapter{Mathematical Gap Matrix - Fine-Grained State}

The following summarizes the current matrix without pretending that every listed operation is universally complete.

\begin{longtable}{@{}p{0.26\textwidth}p{0.17\textwidth}p{0.50\textwidth}@{}}
\toprule
Capability & Status & Explicit limitation / interpretation \\
\midrule
Scalars / finite arithmetic & Implemented, partial migration & Some legacy geometry still performs direct arithmetic. \\
Constants & Implemented & No material P0 gap recorded. \\
Explicit tolerance & Implemented, migration pending & Legacy local epsilons remain; tolerance must be explicit at authoritative boundaries. \\
Vec2/Vec3/Vec4 & Implemented & Duplicate historical Point representations remain. \\
Mat2/Mat3/Mat4 & Implemented, integration pending & Robust rank/condition semantics route through linear algebra authority. \\
Quaternion & Implemented, hardening pending & Very large intermediate rotations require continued overflow hardening. \\
Affine/rigid transforms & Implemented, extension pending & More constructors/property coverage remain possible. \\
Robust LU/QR/SVD & Implemented, hardening pending & Wider null-space and deeper ill-conditioning coverage remain desirable. \\
Predicates & Implemented, extension pending & Broader exact/error-bound escalation remains future work. \\
Analytic primitives & Implemented, extension pending & Broader containment/projection/distance families remain. \\
Analytic intersections & Partial & Primitive cross-product matrix and structured overlap representations remain incomplete. \\
Segments/rays & Partial & Pairwise intersection families remain. \\
Polynomial/roots & Partial & Stable clustered/repeated/complex root authority remains broader than current contract. \\
Interval arithmetic & Partial & Full transcendental/polynomial interval machinery is not claimed. \\
Curve differentials & Partial & Full general Curve2/Curve3 curvature/torsion framework is not universal. \\
NURBS exact operations & Partial & Knot removal/degree elevation/general rational operations remain extensions. \\
Surface differentials & Partial & Broader independent oracle/singular classification remains future. \\
Trim mathematics & Implemented/Tested for certified line/arc domain & General freeform 3D trim correspondence is outside current certification. \\
General distance/projection & Partial & No universal closest-point engine is claimed. \\
General intersections & Partial & Broad curve/curve, curve/surface, surface/surface refinement remains incomplete. \\
Offset/sweep/loft/blend & Implemented/Tested for certified domains & Future extensions require explicit new certified domains. \\
Fillet/chamfer & Implemented/Tested for bounded families & Arbitrary edge fillets/chamfers remain future contracts. \\
B-Rep support & Implemented/Tested for certified planar topology & Curved-face sewing/unrestricted topology is excluded. \\
Solid math & Implemented/Tested for certified planar-face solids & Holed/curved exact decomposition remains outside domain. \\
Boolean & Implemented/Tested for bounded AABB domain & General topology-aware B-Rep Boolean construction is excluded. \\
Constraint equations & Implemented/Tested & New equations require new contracts. \\
Analytic Jacobians & Implemented/Tested & Finite differences are verification only. \\
Nonlinear solver & Implemented/Tested & Damped SVD/least-squares is the supported solver model. \\
Tessellation & Implemented/Tested for declared domains & Tessellation is an approximation with explicit fail-closed depth/policy behavior. \\
Spatial acceleration & Implemented/Tested for declared domains & OBB is intentionally not a semantic requirement. \\
GPU abstraction & Implemented/Tested & No hardware backend is part of the abstraction itself. \\
Metal & Implemented/Tested/Hardware-validated for conservative AABB & Not an authoritative f64 geometry engine. \\
CUDA & Implemented/Tested/Hardware-unvalidated & Requires an actual NVIDIA runner for hardware claims. \\
Cross-backend conformance & Implemented/Tested with declared AABB scope & Broader vector/matrix/NURBS cross-backend equivalence is not certified. \\
Final scale/red-team matrix & Implemented/Tested & M16 covers declared adversarial families; it does not make unsupported mathematics supported. \\
\bottomrule
\end{longtable}

\chapter{GPU Architecture and Conformance}

\pathref{kernel/math/gpu.rs} defines the backend-neutral GPU contract.

The current conceptual dimensions are:
\begin{itemize}
\item Backend: CPU, Metal, CUDA, or Auto selection.
\item Precision: authoritative f64 or explicitly scoped conservative f32 broad-phase capability.
\item Operation: vector/matrix/transform, predicate, curve/surface/NURBS, Jacobian residual, candidate generation, tessellation, as declared.
\item Determinism: bitwise, numerical, or non-deterministic as explicitly declared.
\item Capability: availability, f64 support, determinism, operation set, maximum batch/workgroup constraints.
\end{itemize}

\section{M12 abstraction}
M12 establishes capability discovery, selection policy, batching and memory checks, workgroup/dispatch planning, backend-neutral execution, and conformance comparison. It is not itself a hardware implementation.

\section{M13 Metal}
M13 provides a real Apple Silicon Metal execution path for the certified conservative AABB candidate-generation workload. CPU f64 remains the acceptance/reference authority. Metal f32 broad-phase bounds use outward rounding so an accelerator candidate omission is a correctness failure.

\section{M14 CUDA}
M14 provides real CUDA device-native-f64 AABB candidate generation through the declared feature-gated backend. No hardware status is claimed unless an NVIDIA runner actually executes it.

\section{M15 conformance}
M15 uses a shared adversarial AABB fixture. False positives are acceptable for a conservative candidate set; false negatives are not. Repeated execution must be deterministic for the declared workload.

\section{M16 performance}
The recorded macOS-14 release measurements are:

\begin{tabular}{@{}rrrrrrr@{}}
\toprule
Batch & CPU median us & Metal median us & CPU Mpair/s & Metal Mpair/s & Setup/transfer & Efficiency \\
\midrule
32  & 1.25  & 745.79  & 396.8000 & 0.6651  & 13.16\% & 4.15\% \\
64  & 4.79  & 1260.67 & 420.7012 & 1.5992  & 11.37\% & 9.97\% \\
128 & 17.33 & 1136.58 & 468.9321 & 7.1513  & 7.41\%  & 44.59\% \\
256 & 61.88 & 2035.33 & 527.5152 & 16.0367 & 6.23\%  & 100.00\% \\
512 & 229.75 & 10602.17& 569.3841 & 12.3386 & 2.08\%  & 76.94\% \\
\bottomrule
\end{tabular}

No CPU/Metal crossover occurred in the tested range. This is evidence for that measured workload, not a universal performance theorem.

\chapter{OCCT Boundary}

\pathref{kernel/occt} is an OpenCASCADE-backed native boundary.

The current bridge is C++17 and is built through CMake. The bridge sources include primitive geometry, Boolean operations, extrusion, revolution, loft, fillet, NURBS curve/surface work, differential surface operations, curve/surface operations, section, sweep, sewing, topology evidence, tessellation, exchange, and pathology fixtures.

OCCT may be:
\begin{itemize}
\item an implementation backend;
\item an oracle for independent comparison;
\item a translation/exchange mechanism.
\end{itemize}

OCCT must not become the system-wide semantic authority. A domain contract belongs above the backend.

\chapter{Kernel Contract Crates}

The contract workspace separates stable backend-neutral concepts.

\begin{longtable}{@{}p{0.29\textwidth}p{0.63\textwidth}@{}}
\toprule
Crate & Responsibility \\
\midrule
\texttt{draft-api} & Drafting contract family. \\
\texttt{exchange-api} & STEP/IGES import/export status, evidence, and backend abstraction. \\
\texttt{fillet-api} & Fillet contract family. \\
\texttt{freeform-api} & Freeform surface contract family. \\
\texttt{geometry-api} & Common geometry backend status, kinds, tolerance context, bounding boxes, topology counts, descriptors, validation. \\
\texttt{geometry-contract-tests} & Backend-independent geometry contract tests. \\
\texttt{kernel-integration-api} & Operation identity, phase, cancellation, error classification, and lifecycle semantics. \\
\texttt{mesh-api} & Mesh vertices/triangles/tessellation options and validation. \\
\texttt{nurbs-surface-api} & NURBS surface evaluation and trim surface types. \\
\texttt{offset-api} & Offset contract family. \\
\texttt{reference-evolution-api} & Topology/reference evolution evidence. \\
\texttt{reference-migration-api} & Reference migration contract family. \\
\texttt{shell-api} & Shell/thickening contract family. \\
\texttt{surface-operations-api} & Point/surface, curve/surface, surface/surface, bilinear and splitting operation contracts. \\
\texttt{sweep-api} & Sweep contract family. \\
\texttt{topology-api} & Topology IDs, edge uses, shell orientation/connectivity, containment graphs, repair/snapshot structures. \\
\bottomrule
\end{longtable}

\section{Geometry status model}
\pathref{kernel/contracts/crates/geometry-api/src/lib.rs} defines \texttt{GeometryStatus}: Success, InvalidInput, BackendFailure, TopologyInvalid, Ambiguous, Indeterminate, Unsupported. That vocabulary is intentionally stronger than a Boolean success/failure flag.

\section{Tolerance context}
The common geometry contract distinguishes modeling and validation tolerances, both finite and non-negative. Tolerance is explicit input rather than hidden global state.

\section{Kernel integration lifecycle}
\pathref{kernel/contracts/crates/kernel-integration-api/src/lib.rs} defines operation kinds Create, Transform, Modify, Boolean, Repair, Import, Export; phases Preconditions, Execute, Validate, Commit; and operation errors Cancelled, InvalidInput, Unsupported, SemanticInvariantViolation, BackendFailure, Internal. The phase model makes "validation after execution but before semantic commit" explicit.
