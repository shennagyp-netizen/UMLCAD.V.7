# V6 Remaining Implementation

S18's bounded topology-reference evolution and migration contract is complete, including its adversarial and determinism gate. Remaining V6 work is advanced kernel expansion, executed as independent semantic contracts and native conformance slices.

## Remaining implementation families

- generalized NURBS/freeform surface intersection tracing and boundary derivation;
- general sweep/pipe operations beyond the currently certified bounded families;
- broader offsets, shell/thickness, blends, and imported-shape healing;
- deterministic tessellation beyond the currently certified fixtures and quality envelope;
- broader STEP/IGES exchange and round-trip coverage;
- generalized arbitrary-B-Rep topology operations and pathology handling;
- kernel API/integration expansion while preserving immutable/stateless semantic authority;
- subsequent assembly, motion, and engineering-analysis foundations outside the part-geometry kernel boundary.

Each slice must preserve the UMLCAD semantic authority boundary, remain backend-neutral at the contract layer, fail closed for unsupported cases, and pass independent semantic, native-conformance, adversarial, determinism, release, and authoritative CI gates before being declared complete.
