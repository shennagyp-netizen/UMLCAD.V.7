# UMLCAD V6 NURBS Surface API

Backend-neutral tensor-product rational NURBS surface definition and conformance contract.

The semantic definition is independent of any geometric kernel implementation. A backend implements `NurbsSurfaceBackend` to realize the definition as native geometry.
