\documentclass[11pt,a4paper]{report}

\usepackage[a4paper,margin=22mm,headheight=14pt]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath}
\usepackage{array}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{ragged2e}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,calc}
\usepackage[hidelinks]{hyperref}
\usepackage{fancyhdr}
\usepackage{titlesec}

\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}
\setlist{leftmargin=*,itemsep=2pt,topsep=2pt}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\setlength{\emergencystretch}{2em}
\definecolor{codegray}{gray}{0.96}
\definecolor{rulegray}{gray}{0.55}
\lstset{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{codegray},
  frame=single,
  rulecolor=\color{rulegray},
  breaklines=true,
  breakatwhitespace=false,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  xleftmargin=4pt,
  xrightmargin=4pt
}
\newcommand{\pathref}[1]{\url{#1}}
\newcommand{\status}[1]{\textbf{#1}}
\newcommand{\current}{\textit{Current main implementation}}
\newcommand{\target}{\textit{System-CAD target architecture}}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{UMLCAD.V.7}
\fancyhead[R]{System Architecture Handbook}
\fancyfoot[C]{\thepage}

\titleformat{\chapter}{\LARGE\bfseries}{\thechapter}{10pt}{}
\titleformat{\section}{\Large\bfseries}{\thesection}{8pt}{}
\titleformat{\subsection}{\large\bfseries}{\thesubsection}{6pt}{}

\begin{document}

\begin{titlepage}
\centering
\vspace*{18mm}
{\Huge\bfseries UMLCAD.V.7\par}
\vspace{5mm}
{\LARGE System Architecture, Mathematical Authority, Engineering Domains, Contracts,\par}
{\LARGE Runtime Boundaries, Validation, and Repository Reference\par}
\vspace{15mm}
{\large Comprehensive engineering handbook\par}
\vspace{6mm}
{\large Audited against the exact repository state immediately before this document revision:\par}
\vspace{2mm}
{\ttfamily 2451b765eb1e1e3177b965ffa5ab23aa35a51995\par}
\vfill
{\large The repository is the source of truth.\par}
{\large This document distinguishes implemented code from target architecture.\par}
\vspace{8mm}
{\large September 19, 2026\par}
\end{titlepage}

\tableofcontents
\clearpage

\chapter{Document Purpose and Reading Rules}

This handbook documents the architecture and engineering state of UMLCAD.V.7 as a whole. It is deliberately more precise than a marketing overview and more compact than a raw source dump. The document describes the repository as an engineering system: authority, ownership, contracts, state transitions, deterministic identity, mathematical guarantees, provider boundaries, testing, deployment shape, and known limits.

There are two separate truths that must never be confused:

\begin{tabularx}{\textwidth}{@{}p{0.23\textwidth}X@{}}
\toprule
Term & Meaning \\
\midrule
\current & Code and files verified to exist in the current \texttt{main} repository state. \\
\target & Architectural direction expressed by the current development law and the architecture implied by the requested System-CAD model. It may not yet have an implementation on \texttt{main}. \\
\bottomrule
\end{tabularx}

A class, interface, endpoint, test, or documentation statement is not by itself proof that a capability is complete. Completion requires the semantic path, authoritative behavior, failure semantics, determinism, required tests, and applicable end-to-end evidence.

The mathematical roadmap M0-M16 is closed for its declared certified domains. That closure means the mathematical authority is treated as the existing foundation for the next system-CAD layer; it does not mean every possible CAD mathematical operation is implemented.

\section{Repository fact at the audit point}
At the audit point, the repository contains the mature Rust mathematical/native foundation, the existing \texttt{UMLCAD.Framework} and \texttt{UMLCAD.Kernel.Client} .NET surface, the legacy TypeScript kernel API, the OCCT backend/contracts, the demo/E2E harness, and the CI workflows described later. Some newer System-CAD domain projects described in earlier handbook text are not present on the exact current \texttt{main} tree and are therefore documented here as target architecture rather than current implementation.

\chapter{Executive Architecture}

The stable conceptual architecture is:

\begin{center}
\begin{tikzpicture}[
  box/.style={draw,rounded corners,align=center,minimum width=43mm,minimum height=10mm,font=\small},
  a/.style={box,fill=blue!6}, b/.style={box,fill=green!7}, c/.style={box,fill=orange!9}, d/.style={box,fill=gray!10},
  >=Latex
]
\node[a] (platform) {Platform Foundation};
\node[a,below=5mm of platform] (math) {Rust Mathematical Authority};
\node[b,below=5mm of math] (science) {Science};
\node[b,below=5mm of science] (cad) {CAD Core / Product Semantics};
\node[c,below=5mm of cad] (resources) {Engineering Resources};
\node[c,below=5mm of resources] (domains) {Specialized Engineering Domains};
\node[d,below=5mm of domains] (app) {Application / Workflow / Presentation};
\draw[->] (platform)--(math);
\draw[->] (math)--(science);
\draw[->] (science)--(cad);
\draw[->] (cad)--(resources);
\draw[->] (resources)--(domains);
\draw[->] (domains)--(app);
\end{tikzpicture}
\end{center}

The important point is not that every layer must be one project. The point is ownership and dependency direction.

\section{Authority hierarchy}

\begin{enumerate}
\item \textbf{Rust mathematics}: normative numerical authority for the mathematical contracts it declares and certifies.
\item \textbf{System-CAD semantics}: authoritative owner of CAD meaning, product meaning, engineering intent, references, evaluation orchestration, lifecycle state, and downstream domain composition.
\item \textbf{Providers/adapters}: implementation mechanisms behind stable service contracts.
\item \textbf{Representation/viewer}: derived consumer assets only; never authority.
\end{enumerate}

The Rust kernel is intentionally thin. The complete CAD system must not be compared with it as though they were equivalent architectural layers.

\chapter{C4 Level 1 - System Context}

\begin{center}
\begin{tikzpicture}[
  ext/.style={draw,rounded corners,align=center,minimum width=27mm,minimum height=9mm,font=\scriptsize,fill=gray!10},
  sys/.style={draw,rounded corners,align=center,minimum width=38mm,minimum height=11mm,font=\scriptsize,fill=blue!8},
  >=Latex
]
\node[ext] (engineer) {Engineer / System User};
\node[sys,right=12mm of engineer] (cadapp) {UMLCAD.V.7\ System-CAD};
\node[sys,below=11mm of cadapp] (kernel) {Rust Mathematical\ Kernel};
\node[ext,right=12mm of cadapp] (providers) {Engineering / Scientific\ Providers};
\node[ext,below=10mm of providers] (plm) {PLM / PDM / ERP};
\node[ext,below=10mm of engineer] (machine) {Manufacturing Machine / Controller};
\draw[->] (engineer)--node[above]{design / inspect} (cadapp);
\draw[->] (cadapp)--node[right]{contracted mathematics} (kernel);
\draw[<->] (cadapp)--node[above]{provider contracts} (providers);
\draw[<->] (cadapp)--node[right]{lifecycle / product data} (plm);
\draw[->] (cadapp)--node[left]{deterministic NC} (machine);
\end{tikzpicture}
\end{center}

\target{} treats PLM, simulation, machine-control, and other external systems as integrations rather than hidden dependencies inside CAD semantics.

\chapter{C4 Level 2 - Containers}

\begin{tabularx}{\textwidth}{@{}p{0.24\textwidth}X@{}}
\toprule
Container / subsystem & Responsibility \\
\midrule
\pathref{kernel/math} & CPU mathematical authority: scalar, tolerance, vector/matrix, analytic geometry, curves/NURBS, surfaces, intersections, constraints/Jacobian/solver, B-Rep/solid math, tessellation, spatial acceleration, GPU contracts, and related diagnostics. \\
\pathref{kernel/native} & Rust host and public kernel API. Dispatches typed requests to mathematical services and owns native GPU integration. \\
\pathref{kernel/contracts} & Backend-neutral Rust contracts for geometry, topology, mesh, exchange, surface operations, references, construction, and kernel lifecycle/integration behavior. \\
\pathref{kernel/occt} & OCCT-backed implementation/oracle layer. It may realize or challenge contracts but is not semantic authority. \\
\pathref{kernel/api} & Legacy TypeScript API/application surface. Useful for compatibility/history; it is not the long-term semantic authority. \\
\pathref{dotnet/src/UMLCAD.Framework} & Current .NET semantic/build framework: application builder, product-like definitions, assemblies, drawings/sheets, metadata, configuration, deterministic build identity, build history, package creation, and compiled model manifest generation. \\
\pathref{dotnet/src/UMLCAD.Kernel.Client} & Current .NET transport adapter to the Rust kernel. Handles timeout, response limits, JSON, stable diagnostics, cancellation, and compiled-result validation. \\
\pathref{projects/demo} & Executable demonstration and integrated runtime path. The E2E mode starts the kernel and validates the compiled model without opening the viewer. \\
\pathref{tests/e2e} & System-level process/wire gate and red-team harness. \\
\pathref{viewer} & Presentation boundary. The current repository proves the viewer package manifest and demo integration assumptions; implementation details that are not present on the exact \texttt{main} tree are not claimed here as implemented. \\
\bottomrule
\end{tabularx}

\chapter{System-CAD Domain Architecture}

The long-lived System-CAD architecture is organized around meaning rather than a list of applications.

\section{Core semantic ownership}

CAD Core owns, where applicable:

\begin{itemize}
\item Product, Part, Body, Sketch, Feature, Assembly, Occurrence, and product-structure semantics.
\item Design intent, parameters, constraints, references, publications, and configuration/context.
\item Coordinate-frame semantics and transforms.
\item Evaluation planning and dependency closure.
\item Deterministic semantic/evaluation/result identity.
\item Cache and invalidation semantics.
\item Authoritative results, provenance, topology evolution, and diagnostics.
\item Drawing and PMI meaning.
\item Product Structure and BOM semantics.
\item Integration contracts consumed by Sheet Metal, CAM, Simulation, and PLM.
\end{itemize}

The mathematical kernel supplies computations, not these meanings.

\section{Stable relationship model}

The requested relationship is an architecture invariant:

\begin{center}
\begin{tikzpicture}[
  node distance=7mm and 13mm,
  box/.style={draw,rounded corners,align=center,minimum width=37mm,minimum height=9mm,font=\small},
  >=Latex
]
\node[box,fill=green!8] (science) {Science\\Material / physical properties\\Phenomena Simulation Service};
\node[box,fill=orange!8,right=of science] (resources) {Engineering Resources\\Machine / Tool / Fixture\\Process / Capabilities};
\node[box,fill=blue!7,below=of science] (sheet) {Sheet Metal};
\node[box,fill=blue!7,below=of resources] (cam) {CAM\\Toolpath / Postprocessor\\Deterministic G-code / NC};
\node[box,fill=gray!10,below=15mm of $(sheet)!0.5!(cam)$] (product) {CAD Product Structure};
\node[box,fill=gray!10,below=of product] (bom) {BOM};
\node[box,fill=gray!10,below=of bom,minimum width=75mm] (consumers) {Drawing / CAM / PLM};
\draw[<->] (science)--(resources);
\draw[->] (resources)--(sheet);
\draw[->] (resources)--(cam);
\draw[->] (science)--(cam);
\draw[->] (science)--(sheet);
\draw[->] (product)--(bom);
\draw[->] (bom)--(consumers);
\draw[->] (product)--(consumers);
\end{tikzpicture}
\end{center}

The crossing arrows above express consumption relationships, not inheritance and not ownership transfer.

\section{Science}
Science owns scientific facts and services that can be reused by multiple engineering domains. Material and physical properties belong here. The Phenomena Simulation Service is an abstract, provider-neutral service boundary.

\section{Phenomena Simulation Service}

The stable boundary is:

\begin{lstlisting}
Engineering Consumer
        -> IPhenomenaSimulationService
        -> IPhenomenaSimulationProvider
        -> Simulation Implementation
\end{lstlisting}

The service does not encode whether the provider is internal, external, CPU-based, GPU-based, specialized, or remote. Provider identity, execution detail, and transport belong behind the contract unless the service explicitly makes them semantic inputs.

A simulation result must carry enough identity, inputs, assumptions, and provenance for the consumer to decide whether the result is applicable. A visually plausible scalar without provenance is not an authoritative engineering result.

\section{Engineering Resources}

Engineering Resources own reusable manufacturing/engineering facts and capability declarations:

\begin{itemize}
\item Machine.
\item Tool.
\item Fixture.
\item Process.
\item Capabilities.
\end{itemize}

CAM and Sheet Metal consume these resources; the resource model must not become hidden CAM state.

\section{Sheet Metal}

Sheet Metal owns sheet-metal design semantics. It consumes engineering resources and applicable science/simulation contracts. Material/machine/process feasibility validation is a cross-domain responsibility implemented through explicit contracts, not by allowing Sheet Metal or CAM to become a second CAD kernel.

\section{CAM}

CAM consumes authoritative CAD semantics, product structure/BOM, engineering resources, material/process information, applicable Sheet Metal outputs, and the Phenomena Simulation Service where needed.

The manufacturing chain is:

\begin{lstlisting}
Manufacturing Definition
        -> Toolpath
        -> Postprocessor
        -> Deterministic G-code / NC
\end{lstlisting}

The postprocessor is a semantic boundary. Machine configuration, tool, fixture, process, coordinate system, operation order, postprocessor version/configuration, and other declared deterministic inputs must participate in the relevant identity. A renderable toolpath is not itself machine-ready NC.

\section{CAD Product Structure and BOM}

Product Structure owns product composition. BOM is a derived semantic view:

\begin{lstlisting}
CAD Product Structure
        -> BOM
        -> Drawing / CAM / PLM
\end{lstlisting}

BOM must not be a competing source of truth. Quantities, variants, configuration, occurrence identity, and product references remain traceable to the authoritative product state.

\section{Drawing and PMI}

Drawing consumes authoritative CAD and product semantics. It may derive geometry, sections, dimensions, annotations, and BOM views for documentation, but it must not reconstruct engineering truth from presentation meshes. Drawing details are mapped against the declared external capability set; the viewer remains downstream.

\section{PLM}
PLM consumes authoritative product, revision, lifecycle, configuration, and related engineering metadata. It may own lifecycle/workflow records but does not become geometric authority.

\chapter{Specification - Evaluation - Result - Representation}

A central invariant is:

\begin{center}
\begin{tikzpicture}[node distance=11mm and 9mm,box/.style={draw,rounded corners,align=center,minimum width=30mm,minimum height=10mm,font=\scriptsize},>=Latex]
\node[box,fill=blue!7] (s) {Specification\\Design intent};
\node[box,fill=orange!8,right=of s] (e) {Evaluation\\Dependency-aware execution};
\node[box,fill=green!8,right=of e] (r) {Authoritative Result\\Identity / evidence / provenance};
\node[box,fill=gray!10,right=of r] (p) {Representation\\Derived consumer asset};
\draw[->] (s)--(e);\draw[->] (e)--(r);\draw[->] (r)--(p);
\end{tikzpicture}
\end{center}

\section{Specification}
Specification stores intent: definitions, parameters, constraints, references, configuration, declared relationships, and source identity. It should not store hidden mutable evaluation state.

\section{Evaluation}
Evaluation resolves dependencies, constructs deterministic requests, calls the canonical evaluator/adapter, integrates authoritative results, and computes derived assets when required.

\section{Authoritative Result}
An authoritative result is trusted semantic evidence. It must identify what was evaluated, under which inputs and configuration, with which authority, status, and provenance. Topology and reference correspondence are evidence, not guesses.

\section{Representation}
Representation is disposable consumer data. It may be regenerated without changing semantic truth. Rendering may use approximations that are unacceptable as authoritative geometry.

\chapter{References, Topology, Frames, and Identity}

\section{Reference semantics}
A reference is semantic. It is not an accidental array index. The system must be able to state the producer, expected result identity, semantic target, frame requirements, and selector restrictions.

Required resolution states are:
\begin{lstlisting}
Resolved
Missing
Ambiguous
Indeterminate
Unsupported
\end{lstlisting}
Arbitrary fallback is forbidden.

\section{Topology provenance}
When topology changes, a downstream consumer must have explicit correspondence/evolution evidence. Topology array order is never semantic identity.

The mathematical layer contains explicit topology support for vertices, edges, coedges, wires, faces, shells, incidence, orientation, closure, shell connectivity, and certified planar/solid domains. Broader curved-face sewing and unrestricted topology construction remain separate contracts.

\section{Coordinate-frame semantics}
Where applicable, distinguish World, Document, Part, Body, Sketch, Face, Occurrence, Drawing View, and Simulation frames. Feature direction is an oriented semantic value, not an interpretation of viewer camera state.

\section{Deterministic identities}
The current .NET Framework canonicalization is explicit:
\begin{enumerate}
\item validate required IDs and definitions;
\item sort configuration and semantic collections by ordinal identity;
\item serialize canonical semantic content using camel-case JSON without indentation;
\item compute SHA-256 over canonical bytes;
\item attach the resulting lowercase hexadecimal value as \texttt{BuildIdentity}.
\end{enumerate}
The canonical semantic schema is \texttt{uml-cad-semantic/1.2.0}; the current bridge package is \texttt{uml-cad-build-package/1.0.0}; the compiled model schema is \texttt{uml-cad-compiled-model/1.1.0}.

\chapter{Current .NET Framework}

\section{Application builder}
\pathref{dotnet/src/UMLCAD.Framework/CadApplication.cs} defines the current public application builder.

\begin{tabularx}{\textwidth}{@{}p{0.29\textwidth}X@{}}
\toprule
API / concept & Current behavior \\
\midrule
\texttt{CadApplication.CreateBuilder()} & Creates the application builder. \\
\texttt{ApplicationId} & Non-whitespace identifier; default \texttt{uml-cad-application}. \\
\texttt{Version} & Non-whitespace identifier; default \texttt{1.0.0}. \\
\texttt{AddPart} & Adds a part definition with parameters, geometry descriptors, constraints, references, components, and metadata. \\
\texttt{AddAssembly} & Adds an assembly definition with typed occurrences and nested assemblies. \\
\texttt{AddDrawing} & Adds a drawing with sheets, part references, and settings. \\
\texttt{Build()} & Validates definitions, builds DI, canonicalizes state, computes SHA-256 identity, creates the semantic snapshot, records history, and returns the application. \\
\texttt{CreateBuildPackage()} & Produces the current package used by the existing kernel-client boundary. \\
\texttt{CreateCompiledModelManifest()} & Compiles semantic application state into a deterministic graph manifest. \\
\bottomrule
\end{tabularx}

\section{Builder validation}
The current builder rejects empty/whitespace required values, duplicate definition IDs across parts and assemblies, duplicate occurrence IDs within an assembly, references to unknown definitions, and circular assembly references.

\section{Part semantics}
A part contains sorted parameters, geometry descriptors, constraints, references, components, part type, name, and structured metadata. Metadata supports part number, description, material, manufacturer, vendor, revision, lifecycle state, author, document code, and custom properties.

The current Framework geometry/constraint descriptors are semantic descriptions. They are not the Rust mathematical geometry types. This is the intended separation between system meaning and mathematical execution.

\section{Assembly semantics}
Assemblies contain occurrences. Each occurrence has definition identity/type, stable name/ID, transform, metadata, optional configuration, quantity, BOM structure, visibility/suppression, grounded state, and flexibility state. Nested assemblies are recursively represented.

\section{Drawing semantics}
A drawing contains stable sheets, part references, and settings. Sheets contain IDs, names, drawing references, and settings.

\section{Build history}
\pathref{dotnet/src/UMLCAD.Framework/Semantics/BuildHistory.cs} stores build snapshots in a synchronized in-memory history. Sequence numbers identify history entries and are deliberately not used as semantic build identity.

\chapter{Compiled Model Contract}

\pathref{dotnet/src/UMLCAD.Framework/Semantics/CompiledModel.cs} defines the current compiled-result graph.

\section{Package}
\texttt{CompiledModelPackage} contains schema, application identity, build identity, manifest, optional render artifact, and diagnostics.

\section{Manifest}
\texttt{CompiledModelManifest} contains root node IDs, nodes, relationships, representations, topology bindings, source bindings, diagnostics, and repeated application/build identity.

\section{Nodes and relationships}
\texttt{CompiledNode} carries ID, name, kind, parent/children, metadata, relationship IDs, representation IDs, capabilities, optional source, and state. \texttt{CompiledRelationship} carries a stable relationship ID, source ID, target IDs, kind, and metadata.

\section{Representations and provenance}
\texttt{CompiledRepresentation} identifies a consumer artifact, render node, bounds, capabilities, and selectable sub-targets. \texttt{CompiledTopologyBinding} bridges a semantic node to a render primitive and includes topology kind, artifact identity, naming/nomenclature, optional number, bounds, and metadata. This bridge never becomes geometry authority.

\section{Graph construction}
The compiler:
\begin{enumerate}
\item emits parts and assemblies in stable ordinal ID order;
\item creates URI-encoded definition IDs;
\item creates deterministic occurrence IDs;
\item recursively emits nested assembly and part children;
\item emits explicit constraint-reference relationships;
\item identifies root definitions not referenced by another assembly;
\item validates parent/child consistency and relationship resolution;
\item rejects duplicate node IDs.
\end{enumerate}
URI encoding prevents colons/slashes in user identifiers from causing structural collisions.

\chapter{Kernel Client Boundary}

\pathref{dotnet/src/UMLCAD.Kernel.Client/RustKernelService.cs} is the current .NET HTTP adapter.

\section{Configuration}
\begin{tabularx}{\textwidth}{@{}p{0.36\textwidth}X@{}}
\toprule
Setting & Current behavior \\
\midrule
\texttt{BaseAddress} & Default \texttt{http://localhost:8080/}; must be absolute HTTP(S). \\
\texttt{EvaluatePath} & Default \texttt{v1/build/evaluate}; must remain relative. \\
\texttt{RequestTimeout} & Default two minutes; must be greater than zero and no more than one hour. \\
\texttt{MaxResponseBytes} & Default 256 MiB; must be greater than zero and no more than 2 GiB. \\
\bottomrule
\end{tabularx}

\section{Failure mapping}
Stable diagnostics include \texttt{KERNEL\_RESPONSE\_TOO\_LARGE}, \texttt{KERNEL\_HTTP}, \texttt{KERNEL\_EMPTY\_RESPONSE}, \texttt{KERNEL\_TRANSPORT}, \texttt{KERNEL\_TIMEOUT}, \texttt{KERNEL\_INVALID\_JSON}, \texttt{KERNEL\_CLIENT}, and \texttt{KERNEL\_INVALID\_RESULT}.

Caller cancellation is propagated. A response body is size-limited while streaming, not merely after deserialization.

\section{Result validation}
The client validates submitted build identity, manifest identity, application identity/version, unique node IDs, root resolution, relationship source/target resolution, and render-artifact build identity. Therefore valid JSON is not equivalent to a valid result.

\chapter{Rust Kernel Public API}

\pathref{kernel/native/src/api/mod.rs} defines a compact typed Rust request/response surface. The current request operations are:

\begin{tabularx}{\textwidth}{@{}p{0.30\textwidth}X@{}}
\toprule
Operation & Purpose \\
\midrule
\texttt{Validate} & Validate a semantic snapshot and report diagnostics. \\
\texttt{AnalyzeLinearSystem} & Analyze/solve a Jacobian-residual linear system under damping and rank tolerance. \\
\texttt{Solve} & Solve supported constraint/relation equations with typed options. \\
\texttt{Analyze} & Produce constraint/relation analysis. \\
\texttt{EngineeringEvidence} & Produce engineering structural/reference/spatial/export evidence. \\
\texttt{Dimensions} & Evaluate declared dimensions. \\
\texttt{ExportDxf} & Produce DXF output. \\
\bottomrule
\end{tabularx}

\section{Native dispatch}
\pathref{kernel/native/src/services/engine.rs} dispatches each typed request to the corresponding mathematical service. The public surface is intentionally much smaller than the internal mathematical module tree.

\chapter{Rust Snapshot and Constraint Semantics}

\pathref{kernel/math/snapshot.rs} defines the data structure consumed by the native mathematical API.

\section{Geometry}
Current exact semantic geometry includes points/lines/circles/arcs represented in the Rust mathematical geometry module. Geometry items have stable semantic IDs and parameter dependencies.

\section{Constraints}
The current constraint families include horizontal, vertical, coincident, fixed, and distance constraints.

\section{Relations}
The relation system includes parallel, perpendicular, equal-length, angle, collinear, concentric, equal-radius, radius, diameter, tangent, midpoint, point-on-line, point-on-circle, distance-points, symmetry, and related declared families.

\section{Solver authority}
The production solver uses analytic Jacobian rows for the supported equation set. Finite differences remain an independent verification mechanism and are not the production Jacobian authority.

\chapter{Mathematical Authority - M0 to M16}

The mathematical roadmap is closed for the explicit certified domains recorded in \pathref{docs/MATH_AUTHORITY_ROADMAP.md} and \pathref{kernel/math/GAP_MATRIX.md}. Closure must not be interpreted as universal coverage of every possible CAD operation.

\begin{longtable}{@{}p{0.07\textwidth}p{0.31\textwidth}p{0.49\textwidth}@{}}
\toprule
ID & Family & Certified / recorded state \\
\midrule
M0 & Audit and baseline & Repository/math inventory and authority baseline. \\
M1 & Scalars, constants, tolerance & Finite-safe scalar foundation, constants, explicit tolerance policy. \\
M2 & Vectors, matrices, quaternion, transform & Vec2/Vec3/Vec4, matrices, quaternion, affine/rigid transforms. \\
M3 & Analytic geometry and predicates & Analytic primitives and scale/degeneracy predicates. \\
M4 & Polynomial and interval & Polynomial/root and interval foundations; not universal algebraic certification. \\
M5 & Curves & Bezier, B-spline, NURBS, and differential foundations. \\
M6 & Surfaces and NURBS & Parametric/NURBS surface and differential/trim foundations. \\
M7 & Distance/projection/intersection & Declared primitive and bounded distance/projection/intersection families. \\
M8 & Construction mathematics & Certified convex/analytic construction families: offset, extrusion, revolution, loft, pipe, blend, frame/twist, bounded fillet/chamfer, shell. \\
M9 & B-Rep and solids & Certified explicit planar topology/solid mathematics and bounded AABB Boolean/split/imprint operations. \\
M10 & Constraints/Jacobian/solver & Supported analytic equations, damped SVD/least-squares, rank/conditioning/terminal authority and adversarial scale evidence. \\
M11 & Tessellation/spatial & Adaptive curves/surfaces, certified convex line/arc trim-aware tessellation, AABB/sphere/subdivision/BVH. \\
M12 & GPU abstraction & Capability/selection/batching/dispatch/executor/conformance contracts. \\
M13 & Metal & Real Apple Silicon conservative AABB candidate generation; hardware-validated. \\
M14 & CUDA & Real device-native-f64 AABB candidate generation; hardware validation not yet recorded. \\
M15 & Cross-backend conformance & Common AABB candidate-set conformance; CPU/Metal hardware evidence; CUDA hardware unvalidated. \\
M16 & Performance/final red team & Release performance evidence and final cross-family adversarial matrix; roadmap closure. \\
\bottomrule
\end{longtable}

\section{Mathematical non-claims}
General concave/holed/freeform trimmed-surface filling, unrestricted curved-face sewing, universal B-Rep Boolean construction, arbitrary edge filleting/chamfering, universal closest-point/intersection engines, and broad accelerator equivalence outside declared contracts are not silently included.

\section{Numerical policy}
CPU \texttt{f64} is the normative reference. GPU implementations accelerate explicit workloads. Tolerances are explicit inputs. Unsupported, ambiguous, singular, degenerate, non-finite, or indeterminate states fail closed.

\section{Solver detail}
\texttt{SolveOptions} currently defaults to maximum 100 iterations, residual tolerance \texttt{1e-8}, step tolerance \texttt{1e-10}, and initial damping \texttt{1e-3}. The finite-difference step remains for API compatibility and independent verification; it is not used to generate production Jacobians.


\begin{itemize}
\item empty deterministic builds;
\item whitespace and malformed required values;
\item global definition-ID uniqueness;
\item duplicate occurrence IDs;
\item unknown references;
\item circular assembly definitions;
\item nested assembly/product structure;
\item metadata, parameters, geometry descriptors, constraints and drawings;
\item compiled node/relationship identity;
\item user-ID collision cases containing slashes and colons;
\item stable kernel-client HTTP/error behavior;
\item invalid JSON, oversized responses and invalid compiled results;
\item real Rust kernel round trips.
\end{itemize}

\section{Black-box kernel integration}
\pathref{dotnet/tests/UMLCAD.Kernel.Integration.Tests/KernelBlackBoxE2ETests.cs} verifies the production client against a real kernel process, including build identity preservation, graph identity, malformed/invalid geometry behavior, stale references, concurrency and protocol failures.

\chapter{Python E2E and Red-Team Harness}

\pathref{tests/e2e/run.py} is the authoritative system-level integration harness.

\section{Single command}
\begin{lstlisting}
python3 tests/e2e/run.py
python3 tests/e2e/run.py --release
python3 tests/e2e/run.py --release --verbose
\end{lstlisting}

\section{Execution sequence}
The harness:
\begin{enumerate}
\item resolves the repository root from its own file location;
\item validates required Rust, .NET and demo paths;
\item runs the complete Rust debug suite and rejects unintended ignored tests;
\item runs the typed Rust kernel API E2E suite;
\item optionally runs the complete Rust release suite;
\item discovers Framework tests and verifies nonzero discovery plus the real-kernel E2E test class;
\item starts the release kernel host and waits for the service socket;
\item runs the complete Framework test suite;
\item runs the black-box Kernel Integration suite;
\item stops the kernel;
\item runs Demo E2E;
\item restarts the kernel for raw HTTP adversarial probes;
\item writes an aggregate JSON report;
\item terminates the kernel process group even on timeout/failure.
\end{enumerate}

The report schema is \texttt{uml-cad-e2e-report/1.0.0}.

\section{System coverage}
\pathref{tests/e2e/COVERAGE.md} records the deliberate multi-boundary strategy:
\begin{itemize}
\item typed Rust library API;
\item .NET Framework contract/unit layer;
\item production .NET HTTP boundary;
\item process/wire boundary;
\item Demo application path.
\end{itemize}

No single layer substitutes for the others.

\section{Raw HTTP red team}
Current raw probes include:
\begin{tabularx}{\textwidth}{@{}p{0.30\textwidth}p{0.56\textwidth}@{}}
\toprule
Probe & Required behavior \\
\midrule
Unknown endpoint & Deterministic not-found response. \\
Invalid JSON & Explicit invalid-JSON rejection. \\
Wrong build schema & Schema rejection. \\
Missing semantic payload & Schema rejection. \\
Unsupported geometry & Explicit unsupported-geometry rejection. \\
\bottomrule
\end{tabularx}

The harness also checks concurrency isolation and process cleanup.

\chapter{CI and Hardware Validation}

Current repository workflows include:

\begin{longtable}{@{}p{0.30\textwidth}p{0.23\textwidth}p{0.36\textwidth}@{}}
\toprule
Workflow & Runner & Purpose \\
\midrule
\pathref{.github/workflows/rust-kernel.yml} & ubuntu-latest & Complete Rust kernel test suite. \\
\pathref{.github/workflows/e2e.yml} & ubuntu-latest & Comprehensive release E2E/red-team gate. \\
\pathref{.github/workflows/metal-hardware.yml} & macos-14 & Apple Silicon Metal validation. \\
\pathref{.github/workflows/cuda-hardware.yml} & self-hosted NVIDIA & Explicit CUDA hardware validation. \\
\pathref{.github/workflows/m16-performance.yml} & macos-14 & Release performance evidence. \\
\bottomrule
\end{longtable}

\section{Hardware-status law}
A hardware claim requires actual execution on the required hardware. The absence of a runner is not a product failure; it is \textit{Not validated}.

\section{Current evidence}
M16 records successful Rust, comprehensive E2E/red-team, Metal hardware, and performance validation on its final main merge. CUDA remains hardware-unvalidated because no confirmed NVIDIA execution record is recorded.

\chapter{Red-Team and Failure-Closed Engineering}

The architecture treats invalid state as a first-class engineering outcome.

\section{Negative-state taxonomy}
\begin{tabularx}{\textwidth}{@{}p{0.24\textwidth}X@{}}
\toprule
State & Meaning \\
\midrule
Invalid & The supplied data violates a declared contract. \\
Unsupported & The system deliberately does not implement the requested domain. \\
Ambiguous & Multiple semantic interpretations remain possible. \\
Indeterminate & Available evidence cannot establish the required truth. \\
Failed & An execution/provider/backend operation did not establish the result. \\
Cancelled & Execution was deliberately stopped and must not be represented as success. \\
\bottomrule
\end{tabularx}

\section{Adversarial classes}
Every applicable new boundary should test malformed identifiers, non-finite numerical values, degenerate geometry, invalid frames, stale references, ambiguity, contradictory topology evidence, cache contamination, deterministic collisions, unexpected provider output, transport corruption, invalid schema, partial results, and repeated-evaluation divergence.

\section{No silent success}
A system must never turn:
\begin{lstlisting}
unsupported -> approximate success
ambiguous   -> arbitrary selection
failed      -> plausible geometry
stale       -> cache hit
cancelled   -> successful result
\end{lstlisting}
The absence of proof is itself information that must be preserved.

\chapter{Deterministic Evaluation, Cache, and Recompute}

The System-CAD target uses a canonical semantic evaluation model.

\section{Evaluation identity}
Every input that can change an authoritative result must participate in the relevant identity, including:
\begin{itemize}
\item definition and feature semantics;
\item resolved reference identities;
\item parameters and expressions;
\item configuration/context;
\item upstream result identities;
\item tolerance/numerical policy where semantically relevant;
\item provider/backend contract version where it changes the result;
\item manufacturing resource/process/postprocessor identity where relevant.
\end{itemize}

\section{Incremental equivalence}
For a given final semantic state:
\begin{lstlisting}
FullRecompute(state) == IncrementalRecompute(state)
\end{lstlisting}
The equality means equivalent authoritative semantic result, not identical execution trace.

\section{Cache equivalence}
For a stable evaluation identity:
\begin{lstlisting}
FreshEvaluation(identity) == CacheHit(identity)
\end{lstlisting}

\section{Invalidation}
Invalidation is dependency-graph closure. It is not a collection of ad-hoc cache clears.

\section{Representation delta}
Derived render/manufacturing/document assets should be regenerated only when the authoritative semantic inputs that control them change.

\chapter{Target Engineering Domain Contract Shape}

The following are architectural contracts, not claims that the named projects currently exist on \texttt{main}.

\section{Science}
\textbf{Owns:} material and physical properties; scientific models; reusable scientific services.

\textbf{Provides:} provider-neutral Phenomena Simulation Service.

\section{Engineering Resources}
\textbf{Owns:} machine, tool, fixture, process and capabilities.

\textbf{Provides:} reusable resource definitions and capability queries.

\section{Sheet Metal}
\textbf{Owns:} sheet thickness, bends, flanges, relief, unfolding/folding and flat-pattern semantics as those capabilities become contracted.

\textbf{Consumes:} CAD semantics, material facts, engineering resources, and applicable simulation results.

\section{CAM}
\textbf{Owns:} manufacturing definitions, operations, strategy, toolpath, postprocessor and deterministic machine output.

\textbf{Consumes:} authoritative CAD, Product/BOM, Engineering Resources, Sheet Metal outputs, and applicable Science/Simulation services.

\textbf{Produces:} deterministic G-code/NC at the postprocessor boundary.

\section{Drawing and PMI}
\textbf{Owns:} sheets, views, projections, sections, dimensions, annotations, tolerances, GD\&T, PMI and drafting standards as those contracts are implemented.

\textbf{Consumes:} authoritative CAD/product semantics and derived measurements/results.

\section{Product Structure and BOM}
\textbf{Product Structure owns:} product definitions, assemblies, occurrences, configurations and composition.

\textbf{BOM owns:} the derived product-structure presentation/analysis required by consumers.

\textbf{Downstream consumers:} Drawing, CAM, PLM and other declared integrations.

\section{PLM}
\textbf{Owns:} lifecycle, revision, workflow, release and document/product integration semantics.

It must reference authoritative product/engineering state rather than becoming a second CAD geometry authority.

\chapter{CATIA Capability Traceability}

CATIA is used as a capability reference, not as the internal architecture to reproduce.

The functional traceability chain is:

\begin{lstlisting}
CATIA capability
    -> UMLCAD semantic contract
    -> domain/service implementation
    -> mathematical contract when needed
    -> authoritative result
    -> derived representation
    -> acceptance evidence
\end{lstlisting}

Major capability families that the System-CAD architecture is designed to accommodate include:

\begin{longtable}{@{}p{0.28\textwidth}p{0.61\textwidth}@{}}
\toprule
Family & Functional scope \\
\midrule
Part Design & Sketch-driven solid features, datum/reference concepts, material removal/addition, patterning, shell/draft/fillet/chamfer and Boolean families. \\
Sketch & Geometry, constraints, relations, support frames, solving, profile loops and publications. \\
Hybrid / Freeform & Curves, surfaces, NURBS, continuity, trim and quality diagnostics. \\
Assembly / Product & Product definitions, occurrences, transforms, constraints, configurations and BOM. \\
Sheet Metal & Thickness, bend, flange, relief, fold/unfold and flat pattern. \\
Kinematics & Constraint-driven motion and relations. \\
Drawing / PMI & Views, sections, dimensions, tolerances, GD\&T, annotations and BOM integration. \\
CAM & Manufacturing operations, toolpaths, simulation integration, postprocessing and deterministic NC/G-code. \\
Knowledge / Configuration & Parameters, formulas, checks, reactions, reusable templates and variants. \\
Lifecycle / Integration & Revision, release, collaboration, PLM/PDM/ERP and external-provider integration. \\
\bottomrule
\end{longtable}

The table describes architectural capability coverage targets, not current implementation completion.

\chapter{Current File-Level Reference}

\section{Root and documentation}
\begin{itemize}
\item \pathref{README.md}
\item \pathref{docs/doc.tex}
\item \pathref{docs/UMLCAD_V7_MAIN_BRANCH_DEVELOPMENT_PROMPT.md}
\item \pathref{docs/MATH_AUTHORITY_ROADMAP.md}
\item \pathref{docs/CONTINUATION_HANDOFF.md}
\item \pathref{kernel/math/GAP_MATRIX.md}
\end{itemize}

\section{Native kernel entry points}
\begin{itemize}
\item \pathref{kernel/native/Cargo.toml}
\item \pathref{kernel/native/src/lib.rs}
\item \pathref{kernel/native/src/api/mod.rs}
\item \pathref{kernel/native/src/api/parity_contract.rs}
\item \pathref{kernel/native/src/bin/kernel_host.rs}
\item \pathref{kernel/native/src/services/engine.rs}
\item \pathref{kernel/native/src/gpu/mod.rs}
\item \pathref{kernel/native/src/gpu/metal.rs}
\item \pathref{kernel/native/src/gpu/cuda.rs}
\item \pathref{kernel/native/src/gpu/conformance.rs}
\end{itemize}

\section{Mathematical entry points}
\begin{itemize}
\item \pathref{kernel/math/mod.rs}
\item \pathref{kernel/math/geometry.rs}
\item \pathref{kernel/math/scalar.rs}
\item \pathref{kernel/math/tolerance.rs}
\item \pathref{kernel/math/vec.rs}
\item \pathref{kernel/math/vec4.rs}
\item \pathref{kernel/math/mat.rs}
\item \pathref{kernel/math/quaternion.rs}
\item \pathref{kernel/math/transform.rs}
\item \pathref{kernel/math/linalg.rs}
\item \pathref{kernel/math/predicates.rs}
\item \pathref{kernel/math/analytic.rs}
\item \pathref{kernel/math/intersections.rs}
\item \pathref{kernel/math/polynomial.rs}
\item \pathref{kernel/math/interval.rs}
\item \pathref{kernel/math/bezier.rs}
\item \pathref{kernel/math/bspline.rs}
\item \pathref{kernel/math/nurbs.rs}
\item \pathref{kernel/math/nurbs3d.rs}
\item \pathref{kernel/math/nurbs_ops.rs}
\item \pathref{kernel/math/surfaces.rs}
\item \pathref{kernel/math/nurbs_surface.rs}
\item \pathref{kernel/math/nurbs_surface_differential.rs}
\item \pathref{kernel/math/nurbs_surface_ops.rs}
\item \pathref{kernel/math/surface_differential.rs}
\item \pathref{kernel/math/trim.rs}
\item \pathref{kernel/math/distance.rs}
\item \pathref{kernel/math/construction.rs}
\item \pathref{kernel/math/offsets.rs}
\item \pathref{kernel/math/sweeps.rs}
\item \pathref{kernel/math/brep.rs}
\item \pathref{kernel/math/topology.rs}
\item \pathref{kernel/math/solid.rs}
\item \pathref{kernel/math/spatial.rs}
\item \pathref{kernel/math/spatial_accel.rs}
\item \pathref{kernel/math/constraints.rs}
\item \pathref{kernel/math/relations.rs}
\item \pathref{kernel/math/jacobian.rs}
\item \pathref{kernel/math/relation_jacobian.rs}
\item \pathref{kernel/math/linearization.rs}
\item \pathref{kernel/math/linear_consistency.rs}
\item \pathref{kernel/math/conditioning.rs}
\item \pathref{kernel/math/solver.rs}
\item \pathref{kernel/math/solver_status.rs}
\item \pathref{kernel/math/terminal_authority.rs}
\item \pathref{kernel/math/convergence.rs}
\item \pathref{kernel/math/tessellation.rs}
\item \pathref{kernel/math/gpu.rs}
\item \pathref{kernel/math/dimensions.rs}
\item \pathref{kernel/math/diagnostics.rs}
\item \pathref{kernel/math/validation.rs}
\item \pathref{kernel/math/dxf.rs}
\end{itemize}

\section{Backend contracts}
\begin{itemize}
\item \pathref{kernel/contracts/crates/geometry-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/geometry-contract-tests/src/lib.rs}
\item \pathref{kernel/contracts/crates/kernel-integration-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/topology-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/mesh-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/exchange-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/freeform-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/fillet-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/offset-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/sweep-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/shell-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/draft-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/reference-evolution-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/reference-migration-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/surface-operations-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/nurbs-surface-api/src/lib.rs}
\end{itemize}

The active CI definition is represented by:
\begin{itemize}
\item \pathref{.github/workflows/rust-kernel.yml}
\item \pathref{.github/workflows/e2e.yml}
\item \pathref{.github/workflows/metal-hardware.yml}
\item \pathref{.github/workflows/cuda-hardware.yml}
\item \pathref{.github/workflows/m16-performance.yml}
\end{itemize}

\chapter{Repository Dependency Rules}

The architecture follows a simple dependency law:

\begin{lstlisting}
semantic domain
    -> stable contract
    -> adapter/provider
    -> concrete implementation

never:

semantic domain
    -> viewer implementation
semantic domain
    -> concrete vendor backend
semantic domain
    -> duplicate mathematical algorithm
\end{lstlisting}

\section{Current dependency reality}
The current Kernel Client project references Framework. This is an observed transitional implementation dependency, not a statement that future semantic domains should depend directly on transport infrastructure.

The current Framework has no dependency on a concrete Rust implementation. It builds semantic state and package contracts.

The Rust mathematical layer has no dependency on the viewer or .NET application layer.

\section{No circular authority}
A downstream representation may depend on authoritative result data. The authoritative result may not depend on its presentation artifact to establish semantic truth.

A simulation provider may depend on scientific contracts. Science must not depend on one concrete simulation provider.

CAM may consume resources and simulation results. Engineering Resources must not depend on CAM's private implementation.

BOM may be consumed by CAM, Drawing, and PLM. BOM must not become a second product-composition authority.

\chapter{Persistence, Lifecycle, and History}

The current Framework's \texttt{BuildHistory} is in-memory application history. It provides:
\begin{itemize}
\item monotonically assigned sequence values;
\item timestamped snapshots;
\item access to the current snapshot;
\item full history retrieval;
\item lookup by sequence;
\item explicit recording of package identity.
\end{itemize}

The sequence is history metadata, not the deterministic semantic identity.

The target System-CAD persistence layer must distinguish:
\begin{itemize}
\item definition identity;
\item revision identity;
\item build/evaluation identity;
\item authoritative result identity;
\item representation identity;
\item lifecycle state;
\item provider/external reference identity.
\end{itemize}

These identities must not be collapsed into one timestamp or one database surrogate key.

\chapter{Coordinate, Transform, and Occurrence Semantics}

The current Framework stores a 4-by-4 transform on assembly occurrences and rejects non-finite values and an invalid homogeneous component.

The System-CAD target must preserve transform semantics across:
\begin{enumerate}
\item part definition coordinates;
\item body/feature coordinates;
\item sketch support frames;
\item assembly occurrence transforms;
\item drawing-view coordinates;
\item manufacturing coordinate systems;
\item simulation coordinate systems.
\end{enumerate}

An occurrence transform is not a geometry mutation of the source part definition. It is contextual placement.

A drawing view is not a new solid. A CAM setup coordinate system is not the source CAD coordinate system. A simulation frame is not a viewer camera.

\chapter{Source Provenance and Auditability}

The compiled model has an explicit source-binding type containing file, symbol, source span, and revision.

The architectural purpose is auditability:

\begin{lstlisting}
consumer result
    -> semantic node
    -> source binding
    -> source revision
\end{lstlisting}

Source provenance must be maintained where it is needed to explain how a result was produced. It must not be used as a substitute for semantic identity.

A source file path is mutable infrastructure metadata; the semantic identity of the modeled object remains independent of the workstation filesystem.

\chapter{Representation and Viewer Boundary}

Representation is downstream of authoritative result.

The current compiled model supports representations and an optional render artifact. The render artifact can carry:
\begin{itemize}
\item artifact identity;
\item build identity;
\item format and media type;
\item asset identity;
\item optional size/integrity metadata;
\item URI or inline data;
\item additional properties.
\end{itemize}

The viewer may select nodes and sub-targets, focus them, hide them, or present them visually. It must not:
\begin{itemize}
\item decide reference correspondence;
\item decide topology identity;
\item decide feature direction;
\item choose a manufacturing process;
\item override material properties;
\item replace a failed authoritative result.
\end{itemize}

The viewer is therefore a consumer of meaning, not a source of meaning.

\chapter{Drawing Architecture}

Drawing is a system-CAD consumer of authoritative CAD state.

The architectural chain is:

\begin{lstlisting}
Authoritative CAD Result
       -> Drawing Views / Sections / Details
       -> Dimensions / Annotations / PMI
       -> BOM view
       -> Drawing exchange
\end{lstlisting}

Drawing dimensions should consume declared measurement contracts. Dimension labels are not a second numerical authority.

Sections, projections, and details must be reproducible from the authoritative model and drawing-view context.

Drawing standards, sheet formats, layers, line styles, tolerances and annotation policies are semantic inputs where they change the produced drawing.

\chapter{Sheet Metal Architecture}

Sheet Metal is a specialized CAD/manufacturing domain.

Its semantic state may include:
\begin{itemize}
\item sheet thickness;
\item bend definitions;
\item bend radius and process rules;
\item flange/feature definitions;
\item relief semantics;
\item material;
\item K-factor or bend-table policy;
\item folded/unfolded state;
\item flat-pattern result;
\item manufacturing/resource constraints.
\end{itemize}

The domain consumes authoritative CAD and Science/Resource contracts.

A flat pattern is a derived authoritative domain result. It must be traceable to the source feature/product state and to the assumptions that generated it.

Sheet Metal may participate in material/machine/process validation, but it does not own Machine or Tool definitions and does not implement a private manufacturing resource database.

\chapter{CAM Architecture}

CAM operates after authoritative product/part semantics are available.

A coherent CAM path is:

\begin{center}
\begin{tikzpicture}[node distance=7mm and 9mm,box/.style={draw,rounded corners,align=center,minimum width=34mm,minimum height=10mm,font=\small},>=Latex]
\node[box,fill=gray!10] (cad) {Authoritative CAD\\Product / BOM};
\node[box,fill=orange!8,right=of cad] (resource) {Engineering Resources\\Machine / Tool / Fixture / Process};
\node[box,fill=green!8,below=of resource] (sim) {Phenomena Simulation\\Service / Provider};
\node[box,fill=blue!8,right=of resource] (cam) {CAM\\Manufacturing Definition};
\node[box,fill=blue!8,below=of cam] (tp) {Toolpath};
\node[box,fill=blue!8,below=of tp] (post) {Postprocessor};
\node[box,fill=gray!10,below=of post] (nc) {Deterministic G-code / NC};
\draw[->] (cad)--(cam);
\draw[->] (resource)--(cam);
\draw[->] (sim)--(cam);
\draw[->] (cam)--(tp);
\draw[->] (tp)--(post);
\draw[->] (post)--(nc);
\draw[->] (cad)--(tp);
\end{tikzpicture}
\end{center}

CAM must preserve enough provenance to answer which part/product state, resource configuration, process definition, coordinate system, and postprocessor produced the NC output.

\section{Simulation in CAM}
CAM may request a physical phenomenon through \texttt{IPhenomenaSimulationService}. The provider implementation remains hidden behind \texttt{IPhenomenaSimulationProvider}.

The simulation result must not silently turn an unvalidated physical assumption into manufacturing authority.

\section{Deterministic NC}
When deterministic output is a requirement, the postprocessor input identity includes all semantic inputs that affect output. Equivalent inputs must produce equivalent machine output.

\chapter{Engineering Resources Architecture}

Engineering Resources form a reusable catalog rather than a CAM-internal object graph.

A resource can expose:
\begin{itemize}
\item stable identity;
\item description and manufacturer metadata;
\item physical or operational limits;
\item capabilities;
\item compatible processes/materials;
\item availability or lifecycle state where that is part of the integration model;
\item configuration/version identity.
\end{itemize}

Examples:
\begin{lstlisting}
Machine
Tool
Fixture
Process
Capability
\end{lstlisting}

A capability is a fact about what a resource can support. It is not itself an instruction to execute.

\chapter{Science and Material Architecture}

Science separates facts from engineering decisions.

Material/physical property data may include the values and models needed by downstream engineering domains. The scientific layer should not encode the proprietary policy of one manufacturing provider.

The architecture supports:

\begin{lstlisting}
Material / Physical Properties
           |
           +----> Engineering domain
           |
           +----> Phenomena Simulation Service
                            |
                     multiple providers
\end{lstlisting}

Provider-independent scientific contracts allow different numerical engines, specialized solvers, remote services, or certified external systems to be substituted without rewriting CAM or Sheet Metal semantics.

\chapter{Application and Workflow Boundary}

The application layer coordinates user workflows and services. It should not silently become the owner of geometry or manufacturing truth.

A future application host may orchestrate:

\begin{lstlisting}
load definition
    -> validate
    -> resolve references
    -> plan dependencies
    -> evaluate
    -> integrate authoritative results
    -> update derived representations
    -> publish document/product/manufacturing outputs
\end{lstlisting}

The current \pathref{projects/demo} is an integration executable, not proof that a general-purpose CAD application host has been implemented.

\chapter{Testing Paradigm}

Every new system-CAD capability should use the following test stack:

\begin{lstlisting}
1. Contract test
2. Authoritative Python E2E RED
3. Minimal implementation
4. Component GREEN
5. Adversarial red-team
6. Metamorphic/property/differential checks where relevant
7. Complete Python E2E
8. Full applicable repository gates
9. Exact-head evidence
\end{lstlisting}

\section{TDD}
The authoritative acceptance test is written or extended before the implementation is considered complete, and it must first produce a meaningful RED result.

\section{Metamorphic testing}
Where exact expected output is impractical, preserve invariants such as:
\begin{itemize}
\item translation/scale laws;
\item repeated evaluation equivalence;
\item input-order canonicalization;
\item cache/fresh equivalence;
\item CPU/accelerator conservative-subset relationships;
\item topology reference preservation.
\end{itemize}

\section{Differential testing}
An independent implementation may challenge the current implementation. For example, OCCT can act as an oracle/back-end comparison for applicable geometry contracts. Agreement does not make the oracle semantic authority.

\section{Property testing}
Property tests should attack invariant families rather than isolated examples: finite input preservation, identity stability, deterministic ordering, monotonic error behavior, containment consistency, and failure-closed behavior.

\chapter{CI Execution Matrix}

The minimum conceptual validation matrix is:

\begin{tabularx}{\textwidth}{@{}p{0.30\textwidth}p{0.26\textwidth}X@{}}
\toprule
Scope & Environment & Evidence \\
\midrule
Rust mathematical suite & Ubuntu & Mathematical unit/regression tests. \\
Rust API E2E & Ubuntu & Typed request/response path. \\
.NET Framework suite & Ubuntu & Semantic/build/client behavior. \\
Black-box kernel integration & Ubuntu + real host & HTTP/process boundary. \\
Python E2E/red-team & Ubuntu & Complete cross-layer workflow and raw protocol attacks. \\
Metal & macOS-14 Apple Silicon & Real conservative GPU workload and repeat determinism. \\
CUDA & Self-hosted NVIDIA & Only source of CUDA hardware evidence. \\
M16 performance & macOS-14 Apple Silicon & Release timing/throughput evidence for declared workload. \\
\bottomrule
\end{tabularx}

A workflow run must be tied to an exact commit. A result from another commit cannot be silently reused as evidence for the current head.

\chapter{Milestone and Evidence Law}

The repository's mathematical milestones M0-M16 are historical/closed for declared mathematical domains. System-CAD development must use the repository's current active milestone/gap documents rather than reconstructing a roadmap from this handbook.

A capability status is one of:

\begin{lstlisting}
Implemented
Tested
E2E-validated
Hardware-validated
Not yet validated
Unsupported
Blocked by infrastructure
\end{lstlisting}

A milestone is GREEN only when every applicable gate has actually executed and passed on the exact head.

\section{Evidence packet}
Each completed increment should identify:
\begin{itemize}
\item exact commit SHA;
\item files changed;
\item contract change;
\item RED command/result;
\item implementation result;
\item GREEN command/result;
\item red-team command/result;
\item E2E command/result;
\item full-gate results;
\item hardware status;
\item known limitations;
\item remaining blockers.
\end{itemize}

\chapter{Known Current Limitations and Non-Claims}

The following are explicit non-claims of the audited current repository:

\begin{enumerate}
\item The repository does not yet contain a full CATIA-scale System-CAD application.
\item The newer specialized .NET Science, Engineering Resources, Sheet Metal, CAM, Drawing, and Simulation integration projects discussed by the architecture are not present on the audited current \texttt{main} tree.
\item A general canonical .NET feature/evaluation engine is not established merely by the Framework build/compiled-model functionality.
\item The current Framework package remains \texttt{uml-cad-build-package/1.0.0}; this document does not promote it into a long-term architectural authority.
\item The legacy TypeScript API remains in the repository and uses a historical V4 implementation dependency; it must not silently become the new System-CAD authority.
\item General curved-face sewing, unrestricted B-Rep Boolean construction, arbitrary fillet/chamfer, universal trim filling, and universal geometric intersection/distance algorithms are outside the declared mathematical certified domains.
\item CUDA hardware is not validated until a real NVIDIA runner executes the feature-gated tests.
\item Metal hardware validation does not make Metal an f64 semantic authority.
\item The viewer is not a geometry authority.
\end{enumerate}

\chapter{Development Discipline for the Next System-CAD Work}

The next implementation target is determined by the repository's current active documents and exact implementation gap.

The agent must not choose work merely because it is a large feature or because a class can be created quickly.

The agent should select the smallest missing semantic boundary whose completion advances the current declared milestone.

The sequence is always:

\begin{lstlisting}
Current main
   -> read current evidence
   -> identify exact gap
   -> write/extend authoritative RED
   -> implement boundary
   -> GREEN
   -> attack boundary
   -> complete E2E
   -> validate exact head
   -> document evidence
\end{lstlisting}

Downstream domains should consume established authoritative CAD results rather than inventing parallel geometry or product truth.

\chapter{Final Architectural Invariants}

\begin{enumerate}
\item The Rust mathematical layer is thin and authoritative only for its explicit mathematical contracts.
\item The .NET System-CAD layer owns CAD meaning, engineering semantics, orchestration, references, lifecycle, and cross-domain composition.
\item Mathematical algorithms are not duplicated merely to avoid an adapter boundary.
\item CPU f64 is the normative mathematical reference where the repository declares it.
\item GPU execution is acceleration, not an alternate semantic authority.
\item OCCT is a backend/oracle and never the system's semantic authority.
\item Specification, Evaluation, Authoritative Result, and Representation remain distinct.
\item References are semantic and fail closed on missing, ambiguous, stale, or indeterminate resolution.
\item Topology array order is never semantic identity.
\item Frame and orientation are explicit semantic inputs.
\item Deterministic identity includes every semantic input that can affect the result.
\item Full and incremental recomputation implement the same semantic function.
\item Cache hits must be equivalent to fresh evaluation for the same identity.
\item Failed, unsupported, ambiguous, invalid, indeterminate, and cancelled states are never silently promoted to success.
\item Science provides reusable scientific facts/services.
\item Phenomena Simulation is provider-neutral and supports multiple providers.
\item Engineering Resources own Machine, Tool, Fixture, Process, and Capabilities as reusable facts.
\item Sheet Metal consumes resources and science and provides authoritative sheet-metal results.
\item CAM consumes CAD/Product/BOM/resources/science and produces deterministic G-code/NC through the postprocessor boundary.
\item Product Structure is the authority for product composition.
\item BOM is derived from Product Structure and consumed by Drawing, CAM, PLM, and other consumers.
\item Drawing consumes authoritative CAD/product state and does not become geometry authority.
\item PLM owns lifecycle/product integration semantics and does not replace CAD authority.
\item The viewer is downstream presentation only.
\item The repository's current milestone documents, not this handbook, determine the next feature sequence.
\item A capability is not complete until its required TDD, red-team, E2E, and full-gate evidence exists.
\end{enumerate}

\chapter*{Revision Note}
This handbook was rewritten to eliminate unstable two-column layout, stale architecture claims, and roadmap coupling. It is intentionally single-column A4 so tables and diagrams remain readable. The architectural relationship between Science, Phenomena Simulation Service, Engineering Resources, Sheet Metal, CAM, Product Structure, BOM, Drawing, and PLM is treated as a stable system relationship rather than a hard-coded implementation sequence.

The mathematical authority record remains anchored to M0-M16 and its explicit certified domains. The active System-CAD implementation state must always be re-audited from the exact \texttt{main} repository head and its current evidence.

\end{document}

\begin{itemize}
\item rejection of whitespace identifiers;
\item global definition ID uniqueness;
\item occurrence uniqueness and reference resolution;
\item nested assemblies;
\item metadata and parameters;
\item deterministic node identity even when user IDs contain slashes/colons;
\item duplicate geometry rejection;
\item stable kernel HTTP/transport/JSON diagnostics;
\item application/build identity round-trips;
\item real Rust kernel E2E responses.
\end{itemize}

\section{Python E2E runner}

\pathref{tests/e2e/run.py} is the authoritative local integration harness. It:

\begin{enumerate}
\item resolves the repository root from the runner location;
\item validates the Rust manifest, both .NET test projects, and the demo project;
\item runs the Rust debug suite and Rust kernel API E2E;
\item optionally runs the full Rust release suite;
\item discovers the Framework test inventory and rejects a zero-test project or missing expected real-kernel tests;
\item starts \texttt{kernel\_host} on 127.0.0.1:8080;
\item runs the complete Framework test project;
\item runs the Kernel black-box integration project;
\item stops the kernel;
\item runs the demo E2E path;
\item restarts the kernel and executes raw HTTP red-team probes;
\item writes a machine-readable \texttt{uml-cad-e2e-report/1.0.0} report;
\item force-cleans the kernel process tree on timeout and always cleans up.
\end{enumerate}

\section{Raw red-team probes}

The current harness attacks at least:
\begin{itemize}
\item unknown endpoint -> 404 and \texttt{KERNEL\_NOT\_FOUND};
\item invalid JSON -> 422 and \texttt{KERNEL\_INVALID\_JSON};
\item wrong build schema -> 422 and \texttt{KERNEL\_BUILD\_SCHEMA};
\item missing semantic section -> 422 and \texttt{KERNEL\_BUILD\_SCHEMA};
\item unsupported geometry kind -> 422 and \texttt{KERNEL\_UNSUPPORTED\_GEOMETRY}.
\end{itemize}

\chapter{CI and Hardware Validation}

The current repository workflows are:

\begin{tabularx}{\textwidth}{@{}p{0.31\textwidth}X@{}}
\toprule
Workflow & Current purpose \\
\midrule
\pathref{.github/workflows/rust-kernel.yml} & Ubuntu Rust kernel test suite. \\
\pathref{.github/workflows/e2e.yml} & Comprehensive Python E2E/red-team gate on Ubuntu with Rust, .NET 10, and Python 3.13. \\
\pathref{.github/workflows/metal-hardware.yml} & Apple Silicon Metal validation on macOS-14. \\
\pathref{.github/workflows/cuda-hardware.yml} & Feature-gated NVIDIA hardware validation on a self-hosted CUDA runner. \\
\pathref{.github/workflows/m16-performance.yml} & Release performance evidence on Apple Silicon. \\
\bottomrule
\end{tabularx}

A workflow that does not execute is neither product PASS nor product FAIL. It is \status{Not validated - infrastructure execution unavailable}. Hardware claims require actual hardware execution.

\section{M16 Apple Silicon evidence}

The recorded M16 evidence on macOS-14 is:

\begin{tabular}{@{}rrrrrrr@{}}
\toprule
Batch & CPU median us & Metal median us & CPU Mpair/s & Metal Mpair/s & Setup/transfer & Efficiency \\
\midrule
32  & 1.25  & 745.79  & 396.8000 & 0.6651  & 13.16\% & 4.15\% \\
64  & 4.79  & 1260.67 & 420.7012 & 1.5992  & 11.37\% & 9.97\% \\
128 & 17.33 & 1136.58 & 468.9321 & 7.1513  & 7.41\%  & 44.59\% \\
256 & 61.88 & 2035.33 & 527.5152 & 16.0367 & 6.23\%  & 100.00\% \\
512 & 229.75 & 10602.17& 569.3841 & 12.3386 & 2.08\%  & 76.94\% \\
\bottomrule
\end{tabular}

No CPU/Metal crossover occurred in the tested range. CUDA hardware remains unverified.

\chapter{Red-Team and Failure-Closed Engineering}

The repository's red-team philosophy is not limited to malformed JSON. Every authoritative boundary must be attacked with the following classes where applicable:

\begin{longtable}{@{}p{0.29\textwidth}p{0.61\textwidth}@{}}
\toprule
Attack class & Required engineering assertion \\
\midrule
Malformed input & Reject explicitly; never parse into plausible defaults. \\
Unsupported input & Return Unsupported or equivalent explicit non-success. \\
Missing reference & Do not invent a target. \\
Ambiguous reference & Do not pick an arbitrary candidate. \\
Stale identity & Reject mismatched producer/result/build identity. \\
Frame mismatch & Reject or explicitly transform through a declared frame contract. \\
Invalid orientation & Do not infer orientation from presentation. \\
Non-finite value & Fail closed. \\
Degenerate geometry & Fail closed unless the contract explicitly defines a valid degenerate case. \\
Tolerance boundary & Test both sides of the declared tolerance; no hidden global epsilon. \\
Cache contamination & Verify cache identity cannot reuse unrelated semantic results. \\
Full/incremental divergence & Require semantic equivalence for the same model state. \\
Dependency order error & Verify deterministic topological evaluation order. \\
Incomplete kernel result & Reject the result as authoritative. \\
Unexpected kernel status & Preserve failure semantics; do not downgrade to success. \\
Transport corruption & Reject invalid HTTP/body/JSON/size states. \\
Provider failure & Preserve provider failure and provenance. \\
Duplicate identity & Reject duplicate semantic/compiled IDs. \\
\bottomrule
\end{longtable}

\chapter{Deterministic Recompute and Cache Model}

The architectural invariants are:

\begin{lstlisting}
FreshEvaluation(EvaluationIdentity)
    ==
CacheHit(EvaluationIdentity)

AuthoritativeResult(FullRecompute(M, Delta))
    ==
AuthoritativeResult(IncrementalRecompute(M, Delta))
\end{lstlisting}

The current Framework already demonstrates canonical ordering and identity. The target System-CAD evaluation engine must extend the same discipline to feature graphs, reference resolution, kernel inputs, topology evolution, downstream domains, and representation deltas.

Incremental recompute is not an alternate semantics. It is an optimization of the same semantic function.

\chapter{Engineering Domain Interfaces - Target Contract Shape}

These interfaces are architectural shapes, not claims that all corresponding projects already exist on \texttt{main}.

\section{Simulation}
\begin{lstlisting}
IPhenomenaSimulationService
    -> IPhenomenaSimulationProvider
    -> Provider implementation
\end{lstlisting}

\section{Manufacturing resources}
\begin{lstlisting}
IEngineeringResourceCatalog
    -> Machine / Tool / Fixture / Process / Capability
\end{lstlisting}

\section{CAM}
\begin{lstlisting}
ICamEngine
    -> ManufacturingDefinition
    -> Toolpath
    -> IPostprocessor
    -> Deterministic G-code / NC
\end{lstlisting}

\section{Product structure and BOM}
\begin{lstlisting}
IAuthoritativeProductStructure
    -> IBomView
    -> Drawing / CAM / PLM consumers
\end{lstlisting}

\section{Drawing}
\begin{lstlisting}
IAuthoritativeCadResult
    -> IDrawingEngine
    -> Derived drawing details / PMI / BOM view
\end{lstlisting}

\section{Reference resolver}
\begin{lstlisting}
IAuthoritativeReferenceResolver
    -> Resolved | Missing | Ambiguous | Indeterminate | Unsupported
\end{lstlisting}

A domain contract should not expose a concrete Rust pointer, a viewer object, or a provider-specific data structure as its semantic model.

\chapter{CATIA Capability Traceability}

CATIA is used as a functional capability reference, not an implementation blueprint.

\begin{longtable}{@{}p{0.27\textwidth}p{0.61\textwidth}@{}}
\toprule
Capability family & UMLCAD system-level trace \\
\midrule
Part Design & Sketch + feature semantics + authoritative evaluation + B-Rep result + derived representation. \\
Sketcher & Geometry, constraints, relations, solver status, frames, profile validity, publications. \\
Hybrid / GSD / Freeform & Curve/surface/NURBS mathematical authority plus higher-level semantic feature orchestration. \\
Sheet Metal & Thickness/bend/flange/relief/fold/flat-pattern semantics plus resource/process validation. \\
Product / Assembly & Product definitions, occurrences, transforms, configurations, engineering relationships, BOM. \\
Kinematics & Constraint-driven motion/behavior services using the mathematical authority. \\
CAM & Manufacturing definitions, operations, toolpaths, simulation integration, postprocessors, deterministic NC. \\
Drawing / PMI & Projection, sections, details, dimensions, tolerances, GD\&T, annotations, BOM views, standards. \\
Knowledge / Configuration & Expressions, checks, reactions, rules, variants, templates, reuse. \\
Lifecycle / PLM / Integration & Product identity, revisions, lifecycle/workflow, PLM/PDM/ERP boundaries. \\
\bottomrule
\end{longtable}

The mapping is by capability. UMLCAD does not need to copy CATIA's internal kernel or module architecture.

\chapter{Current File-Level Reference}

This chapter records the verified source tree in compact groups. Paths that appeared only in older handbook drafts are intentionally not listed as current implementation.

\section{Documentation}
\begin{itemize}
\item \pathref{README.md}
\item \pathref{docs/doc.tex}
\item \pathref{docs/MATH_AUTHORITY_ROADMAP.md}
\item \pathref{docs/CONTINUATION_HANDOFF.md}
\item \pathref{docs/UMLCAD_V7_MAIN_BRANCH_DEVELOPMENT_PROMPT.md}
\item \pathref{kernel/math/GAP_MATRIX.md}
\end{itemize}

The old handbook referenced additional architecture/roadmap files that are not present on the audited current \texttt{main} tree. This revision removes those paths from the claim of current documentation and instead records the architectural target directly.

\section{Native Rust}
\begin{itemize}
\item \pathref{kernel/native/Cargo.toml}
\item \pathref{kernel/native/src/lib.rs}
\item \pathref{kernel/native/src/api/mod.rs}
\item \pathref{kernel/native/src/api/parity_contract.rs}
\item \pathref{kernel/native/src/bin/kernel_host.rs}
\item \pathref{kernel/native/src/services/engine.rs}
\item \pathref{kernel/native/src/services/mod.rs}
\item \pathref{kernel/native/src/gpu/mod.rs}
\item \pathref{kernel/native/src/gpu/conformance.rs}
\item \pathref{kernel/native/src/gpu/metal.rs}
\item \pathref{kernel/native/src/gpu/cuda.rs}
\end{itemize}

\section{OCCT}
\begin{itemize}
\item \pathref{kernel/occt/Cargo.toml}
\item \pathref{kernel/occt/CMakeLists.txt}
\item \pathref{kernel/occt/build.rs}
\item \pathref{kernel/occt/src/lib.rs}
\item \pathref{kernel/occt/cpp/bridge.cpp}
\item \pathref{kernel/occt/cpp/bridge.hpp}
\item \pathref{kernel/occt/cpp/boolean.cpp}
\item \pathref{kernel/occt/cpp/extrusion.cpp}
\item \pathref{kernel/occt/cpp/revolution.cpp}
\item \pathref{kernel/occt/cpp/loft.cpp}
\item \pathref{kernel/occt/cpp/fillet.cpp}
\end{itemize}

\section{Contracts}
\begin{itemize}
\item \pathref{kernel/contracts/crates/geometry-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/geometry-contract-tests/src/lib.rs}
\item \pathref{kernel/contracts/crates/kernel-integration-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/topology-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/mesh-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/exchange-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/freeform-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/fillet-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/offset-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/sweep-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/shell-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/draft-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/reference-evolution-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/reference-migration-api/src/lib.rs}
\item \pathref{kernel/contracts/crates/surface-operations-api/src/lib.rs}
\end{itemize}

\section{Current .NET source}
\begin{itemize}
\item \pathref{dotnet/src/UMLCAD.Framework/CadApplication.cs}
\item \pathref{dotnet/src/UMLCAD.Framework/Configuration/CadConfiguration.cs}
\item \pathref{dotnet/src/UMLCAD.Framework/Semantics/BuildHistory.cs}
\item \pathref{dotnet/src/UMLCAD.Framework/Semantics/BuildPackage.cs}
\item \pathref{dotnet/src/UMLCAD.Framework/Semantics/CompiledModel.cs}
\item \pathref{dotnet/src/UMLCAD.Framework/Semantics/ISemanticRegistry.cs}
\item \pathref{dotnet/src/UMLCAD.Framework/Semantics/SemanticEntity.cs}
\item \pathref{dotnet/src/UMLCAD.Framework/Semantics/SemanticModels.cs}
\item \pathref{dotnet/src/UMLCAD.Framework/Semantics/SemanticServices.cs}
\item \pathref{dotnet/src/UMLCAD.Kernel.Client/RustKernelService.cs}
\end{itemize}

\section{Current .NET project constraints}
\begin{itemize}
\item Target framework: \texttt{net10.0}.
\item Nullable reference types enabled.
\item Implicit usings enabled.
\item \texttt{TreatWarningsAsErrors=true}.
\item Framework uses Microsoft.Extensions configuration, dependency injection, hosting abstractions, options.
\item Kernel Client uses Microsoft.Extensions HTTP/options and references Framework.
\end{itemize}

\section{Tests}
\begin{itemize}
\item \pathref{dotnet/tests/UMLCAD.Framework.Tests/AdversarialEdgeCaseTests.cs}
\item \pathref{dotnet/tests/UMLCAD.Framework.Tests/CompiledModelTests.cs}
\item \pathref{dotnet/tests/UMLCAD.Framework.Tests/IdentityCollisionTests.cs}
\item \pathref{dotnet/tests/UMLCAD.Framework.Tests/RustKernelClientTests.cs}
\item \pathref{dotnet/tests/UMLCAD.Framework.Tests/RustKernelEndToEndTests.cs}
\item \pathref{dotnet/tests/UMLCAD.Framework.Tests/SemanticBuildTests.cs}
\item \pathref{dotnet/tests/UMLCAD.Kernel.Integration.Tests/KernelBlackBoxE2ETests.cs}
\item \pathref{tests/e2e/COVERAGE.md}
\item \pathref{tests/e2e/run.py}
\end{itemize}

\section{CI}

\begin{itemize}
\item \pathref{.github/workflows/rust-kernel.yml}
\item \pathref{.github/workflows/e2e.yml}
\item \pathref{.github/workflows/metal-hardware.yml}
\item \pathref{.github/workflows/cuda-hardware.yml}
\item \pathref{.github/workflows/m16-performance.yml}
\end{itemize}

\section{Viewer / legacy TypeScript}
\begin{itemize}
\item \pathref{viewer/package.json}
\item \pathref{kernel/api/src/index.ts}
\item \pathref{kernel/api/src/application.ts}
\item \pathref{kernel/api/src/contracts.ts}
\item \pathref{kernel/api/src/core.ts}
\item \pathref{kernel/api/src/kernel.ts}
\item \pathref{kernel/api/src/solver.ts}
\item \pathref{kernel/api/server.mjs}
\end{itemize}

\chapter{Repository Dependency Rules}

The safe dependency direction is:

\begin{lstlisting}
Platform
  -> Mathematics
  -> Science
  -> CAD semantics
  -> Engineering resources
  -> Specialized engineering domains
  -> Application / presentation
\end{lstlisting}

Concrete implementation dependencies should point toward stable contracts, not upward into consumers.

The following rules are mandatory:
\begin{itemize}
\item .NET semantic/domain code must not embed a concrete Rust implementation dependency when a contract is sufficient.
\item Viewer code must not become the source of topology, manufacturing, or physical truth.
\item Domain code must not create a hidden second mathematical kernel.
\item OCCT types must not become system-wide semantic types merely because the backend can produce them.
\item Legacy TypeScript APIs must not silently become a competing canonical evaluation engine.
\item Build/package formats retained for compatibility must not automatically become the semantic authority of the next System-CAD layer.
\end{itemize}

\chapter{Documentation, Agent, and Change Discipline}

The repository is shared memory for independent engineering agents.

\section{Every change}

Every substantial change follows:

\begin{lstlisting}
INSPECT
-> AUTHORITATIVE RED
-> IMPLEMENT
-> GREEN
-> RED-TEAM
-> FULL E2E
-> FULL APPLICABLE REPOSITORY GATES
-> EXACT EVIDENCE
\end{lstlisting}

\section{Evidence packet}

A completed increment must record:
\begin{itemize}
\item exact commit SHA;
\item exact commands executed;
\item RED result;
\item targeted GREEN result;
\item unit/integration result;
\item red-team result;
\item Python E2E result;
\item full repository gate result;
\item infrastructure limitations;
\item capability status.
\end{itemize}

\section{Branch discipline}

Agents should work in isolated branches/worktrees. A merge is a technical acceptance event, not merely source aggregation.

\section{No hidden coupling}

Agents communicate through source contracts, tests, fixtures, evidence, and exact commit state. Private reasoning is not an architecture dependency.

\chapter{Definition of Done}

A System-CAD capability is complete only when all applicable gates hold:

\begin{lstlisting}
Semantic contract
AND correct ownership/layer
AND implementation
AND required math contract
AND reference behavior
AND frame/configuration behavior
AND deterministic identity
AND cache/invalidation
AND dependency/update behavior
AND authoritative result
AND provenance/topology evidence
AND derived representation where applicable
AND component tests
AND red-team tests
AND Python E2E
AND full applicable repository validation
AND exact documentation evidence
\end{lstlisting}

Use explicit status labels:

\begin{lstlisting}
Implemented
Tested
E2E-validated
Hardware-validated
Not yet validated
Unsupported
Blocked by infrastructure
\end{lstlisting}

Never use a percentage as a substitute for a gate.

\chapter{Known Limits and Non-Claims}

The following are deliberate non-claims, not omissions in the documentation:

\begin{itemize}
\item M0-M16 closure does not certify arbitrary freeform/curved-face topology construction.
\item General holed/curved exact solid decomposition is not part of the certified solid domain.
\item General topology-aware B-Rep Boolean construction is not certified.
\item OBB is not a required semantic acceleration primitive.
\item Metal is not an authoritative f64 geometry engine.
\item CUDA hardware has not been claimed without an actual NVIDIA runner.
\item Rendering/tessellation output is not semantic authority.
\item The current \texttt{main} tree does not yet verify the full future specialized .NET domain-project set as implemented code.
\item The older TypeScript API is not the architectural excuse to create a second System-CAD semantic stack.
\end{itemize}

\chapter{Final Architectural Invariants}

\begin{enumerate}
\item Rust CPU mathematics is the normative mathematical reference for declared certified domains.
\item .NET system-CAD owns CAD meaning and orchestration.
\item Science owns reusable scientific facts and the provider-neutral Phenomena Simulation Service.
\item Engineering Resources owns Machine, Tool, Fixture, Process, and Capability facts.
\item Sheet Metal and CAM consume engineering-resource information and perform applicable material/machine/process validation through explicit contracts.
\item CAM owns manufacturing-process semantics and deterministically emits G-code/NC through its postprocessor boundary.
\item Product Structure owns product composition.
\item BOM is derived from Product Structure and is consumed by Drawing, CAM, PLM, and other downstream domains.
\item Drawing consumes authoritative CAD meaning; it never creates geometry authority.
\item Simulation providers remain replaceable implementations behind the scientific service boundary.
\item Specification, Evaluation, Authoritative Result, and Representation remain separate.
\item References are semantic, not array-position identities.
\item Topology evolution requires explicit provenance/evidence.
\item Full and incremental recompute must be semantically equivalent.
\item Cache keys are deterministic and complete for authoritative inputs.
\item Failure to prove correctness produces explicit non-success, never plausible substitution.
\item GPU acceleration conforms to CPU authority and never redefines it.
\item OCCT remains an implementation/oracle boundary, never semantic authority.
\item Viewer/presentation is downstream only.
\item The repository's current milestone/gap documents determine what is implemented next; this handbook documents architecture and state rather than imposing a new feature sequence.
\end{enumerate}

\chapter*{Revision Note}
This version replaces the former unstable two-column handbook layout with one stable single-column A4 document. It also removes claims based on paths that were not present on the audited \texttt{main} tree, while preserving the complete mathematical authority record and incorporating the current System-CAD relationship model.

\end{document}